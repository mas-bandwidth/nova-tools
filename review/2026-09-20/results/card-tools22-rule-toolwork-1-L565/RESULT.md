RESULT tools22-rule-toolwork-1-L565 sha=5298f6be12ea
ABSENT
SPEC docs/SPEC-TOOLWORK.md:565 rule 1
PKG internal/docs
ASK The launcher that stages a job must write the job clone's local git config (user.name, user.email, commit.gpgsign=false, core.hooksPath=/dev/null) from the pool's identity.tsv, export GIT_CONFIG_GLOBAL=/dev/null and GIT_CONFIG_NOSYSTEM=1, and refuse a pool whose identity.tsv holds no row.
Quoted deciding lines: the rule itself (docs/SPEC-TOOLWORK.md:565-569): "Staging sets the identity; the worker never does. The launcher writes the job clone's **local** git config — `user.name`, `user.email`, `commit.gpgsign=false`, `core.hooksPath=/dev/null` — from the pool's `identity.tsv` (`owner`, `name`, `email`), and exports `GIT_CONFIG_GLOBAL=/dev/null` and `GIT_CONFIG_NOSYSTEM=1` ... A pool with no identity row is refused at launch."
UNGUARDED
Greps run: `grep -rn "identity.tsv" .` (only docs/SPEC-TOOLWORK.md and a comment at internal/hygiene/hygiene.go:39), `grep -rn "gpgsign|hooksPath|GIT_CONFIG_NOSYSTEM|GIT_CONFIG_GLOBAL" --include='*.go' --include='*.sh' --include='*.lisp'` (non-test hits only internal/hygiene/hygiene.go:497, which is the harvest-side git read env, not launcher staging; profiles/darwin-check.sh is a CI script), `grep -rn "launch-refuses-a-pool-with-no-identity|staged-clone-ignores-the-bench-gitconfig|stage-refuses-a-symlink-out-of-the-job"` (named red tests appear only in SPEC-TOOLWORK.md:611-612, no Test function anywhere), `grep -rn "git config|config user"` over internal/ cmd/ scripts/ tools/ (no launcher writes a local git config). Read: internal/hygiene/hygiene.go, internal/pulse/launch.go, internal/pulse/launchswarm.go, internal/pulse/fill.go, internal/swarm/batch.go, internal/swarm/native.go, cmd/nova-swarm/pull.go (the only git-identity call, -c user.name=nova-swarm, a hardcoded per-command identity at cmd/nova-swarm/pull.go:411, not from identity.tsv), internal/docs/*. Files checked: internal/docs holds doc-conformance tests only, none referencing this rule; SPEC-TOOLWORK.md:996 lists staging (identity.tsv, clone local git config, symlink refusal) as builder card T21 (#1665) "no kind yet". Nothing in the tree reads identity.tsv, writes a job clone's local git config, or refuses a pool without an identity row, so the launcher staging half does not exist here.
Left owed