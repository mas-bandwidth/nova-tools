RESULT: s17pr2-1373 sha=7c29513b0e8f
**DEFECTS**

**What I could not read (said first, because it is the reading):** STEP 1 could not be performed. The base tree the card asserts in `repo/` is not present in this sandbox: `git rev-parse HEAD` fails ("unknown revision", no commits), there is no `repo/` directory anywhere under the readable tree, and the base bundle `/tmp/schema14-ftf.bundle` is outside the sandbox read set (`Operation not permitted`). The sha12 on line 1 is the card's own base-sha, not a locally printed HEAD. Consequently I could not quote laws from `docs/FIXED-FORM-ALGORITHM.md`/`SPEC.md`/`PORTING.md` (STEP 2), could not check the `PORTING.md` matrix (STEP 4), could not inspect the Go emitter or the `ir` package to verify constant spellings/signatures, and could not grep `make/` directly (STEP 5). The diff itself is complete and was reviewed hunk by hunk. The verdict below rests on the card's own measured description of the go-leg gate, which the card instructs me to check this PR against; that measurement is the only ground truth about the gate available here.

**Hunk-by-hunk read (the diff adds one test and three helpers to `internal/codegen/gotable/fixedform_test.go`):**
- Imports (`regexp`, `ir`): coherent with the new code; `ir` is used for the oracle, `regexp` for the emitted-text extraction. Compiles only if `ir.TableFixedRoots/Emitted/WalkRoot/LayoutBytes/LayoutHash` exist — unverifiable, but any mismatch fails loudly at build time if the test is ever run.
- `TestGoFixedFormLayoutAndHashAreIrsByteForByte` body: internally non-vacuous — it f-atals on `len(goLayout)==0`, on `len(want)==0`, on `compared==0`, and CONTROL 1 requires the two fixture roots (`Point`, `Vector`) to emit distinct blocks, so it is not a golden file with extra steps. Every comparison failure path is a loud `Errorf`/`Fatalf`; a regex or emitter-format mismatch would turn the test red, never silently green. The `wantHash` side forces 16 zero-padded lowercase hex digits (`0x%016x`) while the emitted side is whatever the emitter writes, so the two sides agree only if the Go emitter zero-pads to 16 — again a red-loudly fragility, not a false-green.
- `goFixedLayouts`/`byteSpell`: extract `var XxxFixedLayout = []byte{...}` and `const XxxFixedHash = 0x...` and re-spell ir's layout as `0x%02x`-per-byte; the byte-for-byte and number-for-number claim is exactly what is compared. Internally sound; correct only if the emitter names constants `<Root>FixedLayout`/`<Root>FixedHash` keyed on `st.Name` — unverifiable here.

**The defect:**

1. `internal/codegen/gotable/fixedform_test.go:1167` — the PR's entire payload, the new gate test `TestGoFixedFormLayoutAndHashAreIrsByteForByte`, executes in **no** gate job. The go leg's only `go test` is `make/go.mk:557`, whose `-run` filter names exactly four pre-existing tests; the card's measured precedent (schema#1376) establishes that a new fixed-form test in this package runs in no job at all while the package still reports `ok`. This test is added by PR #1373, so it cannot be among the four names already fixed in the filter at base, and per the precedent the filter does not even match a plain `TestFixedForm*` name, let alone `TestGoFixedFormLayoutAndHashAreIrsByteForByte`. The assertion is therefore never run red in any gate target, and the "dump identity of bytes" cell it claims to close (§8, per the PR's own error-message citation of `docs/FIXED-FORM-ALGORITHM.md` §7 item 1 / §8) is scored off a test no job executes. This is the exact shape the card names as a DEFECT in STEP 5: "A case that no gate target reaches is a DEFECT." Offending line, quoted: `func TestGoFixedFormLayoutAndHashAreIrsByteForByte(t *testing.T) {`.

Because the gate never runs it, even the format-fragility points above (emitter hash zero-padding, constant-name keying, `%016x` vs emitter spelling) can drift with the emitter and stay green in CI, so the test's real guarantee — that this leg's emitted layout and hash match the reference byte for byte — is asserted but never exercised by any job this PR touches.