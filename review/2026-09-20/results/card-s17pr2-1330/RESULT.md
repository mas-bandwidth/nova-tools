RESULT: s17pr2-1330 sha=7c29513b0e8f

**CANNOT READ** — The base repo bundle at `/tmp/schema14-ftf.bundle` does not exist on this system, and no other copy of the base tree (commit `7c29513b0e8f`) is reachable from either the working-dir `.git` or the snapshot store at `data/opencode/snapshot/global/834540...`. Without the base tree, none of the referenced law documents (`docs/FIXED-FORM-ALGORITHM.md`, `docs/SPEC.md`, `docs/PORTING.md`, `docs/SPEC-TABLES.md`) can be read, the unmodified lines around the diff hunk cannot be inspected, the PORTING.md matrix cannot be checked, and the make targets cannot be enumerated. The diff itself is complete (one file touched: `test/dart-tables/fixedform.dart`, one new function `rangedScalarClampCase` called from `main`), but all five verification steps require files that are absent.