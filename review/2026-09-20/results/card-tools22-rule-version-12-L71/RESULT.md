RESULT tools22-rule-version-12-L71 sha=5298f6be12ea — does the code at this base do what docs/SPEC-VERSION.md rule 12 says?
ABSENT
SPEC docs/SPEC-VERSION.md:71 rule 12
PKG cmd/nova-version
ASK `moved` and `apply --sha` would each have to print one line, run every child through internal/bounded, take their clock from the injected seam, and touch the network only for the git fetch a named missing revision asks for.
Rule 12's subjects do not exist in the tree. SPEC-VERSION.md opens by defining `nova-version moved` and `nova-update apply --sha` (lines 4-6), and rule 12 says "Both are bounded and clockless" — but neither verb is implemented at this base. The nova-version/nova-update verb dispatch in internal/update/cli.go:146-205 registers help, version, snapshot, diff, release, adoption, report, check, apply and watch only; there is no `moved` verb. The `apply` it does reach is the manifest apply over a named entry with --file, never a --sha build mode: internal/update/cli.go:421 `func apply(entries []Entry, name string, ...)` reads entries, resolves --version, and runs the entry's apply command — no revision build, no stamp. The only `--sha` token anywhere in Go source is a remedy string naming a mode that does not exist: internal/update/snapverb.go:189 "mixed stamps: ... (rebuild the set under one stamp with nova-update apply --sha, or use a --bin per set)". None of the section's red tests exist either.
Greps run: `grep -rn '"moved"' --include='*.go' .` (no hits); `grep -rn --sha --include='*.go' .` (one hit, the remedy string above); `grep -rln "moved" .` (docs only, no Go); `grep -rn "func Test" --include='*_test.go' internal/update/` (no TestMoved* or TestApplySha*; the named red tests TestMovedReadsTheBuildNeverAList .. TestStampCheckAlsoVerifiesSourceMetadata are absent). Files read: cmd/nova-version/main.go (delegates to internal/update.Main), internal/update/cli.go (dispatch and apply), internal/bounded/ (exists, used by the snapshot/report verbs that DO exist, but rule 12 binds moved/apply --sha).
Left owed