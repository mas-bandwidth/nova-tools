RESULT tools22-rule-toolwork-8-L850 sha=5298f6be12ea
CONFORMS internal/merge/read.go:124
SPEC docs/SPEC-TOOLWORK.md:850 rule 8
PKG internal/docs
ASK An implementation must keep the gate's mechanical `ACCEPT OK` out of the read condition entirely, so that a code-changing swarm PR stays `needs_read=yes` until a real reader's unscoped APPROVE for the current head is folded in.
Deciding lines: the read condition is `EvaluateReads` (internal/merge/read.go:55). It reads only the lane's `Read` records, whose verdicts are hold/approve, and an approve counts only when it is a line that is not the author (read.go:106 `sameLine(a.Who, author)`) at the entry's CURRENT head (read.go:64 `if r.Head != e.OID || e.OID == "" { st.Stale++; continue }`), unscoped (read.go:103-105). `ACCEPT OK` never reaches it: it is a PR-body line, and no symbol in cmd/nova-merge or internal/merge references "ACCEPT" at all. `standing` then turns an unsatisfied read into a standing state (internal/merge/plan.go:94-100 `case !reads.Satisfied: return StateNeedsRead, admitted`). On the harvest side a classification can never stand in for a reader: internal/decide/harvestclass.go:322 `func (c Classification) SkipsRead() bool { return false }`, and the harvest cuts a read card for every PR it opens (internal/pulse/harvest.go:307 `appendNext(in.Root, dest.repo, pr, c.Label)`, SPEC-PULSE rule 13).
GUARDED-BY cmd/nova-merge/hold_test.go:552 TestAScopedApproveRecordDoesNotSatisfyNeedsRead
Also pinned by internal/decide/harvestclass_test.go:129 TestAHarvestClassNeverPushesARejectedCard (asserts `c.LiftsHold() || c.SkipsRead()` is false) and internal/pulse/harvest_test.go:353 TestHarvestCutsReadCardPerPR. Verified: the three tests pass.
Greps run: `grep -rn "ACCEPT OK" --include='*.go' .` (no match in code), `grep -rn "ACCEPT" cmd/nova-merge/ internal/merge/ internal/pulse/harvest.go` (no match), `grep -rn "needs_read|NeedsRead|needs-read" --include='*.go' internal/ cmd/`, `grep -rn "TEST-EDIT" .` (docs only), `grep -rn "test-weakened" .` (docs only; the accept gate is T3, not yet built), `grep -rn "designated" --include='*.go' cmd/ internal/`, plus reads of internal/merge/read.go, plan.go, packet.go, cmd/nova-merge/{verbs,land,batch,react}.go and internal/decide/harvestclass.go.
Left owed