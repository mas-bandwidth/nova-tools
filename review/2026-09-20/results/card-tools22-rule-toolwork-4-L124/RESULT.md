RESULT tools22-rule-toolwork-4-L124 sha=5298f6be12ea — does the code at this base do what docs/SPEC-TOOLWORK.md rule 4 says?
ABSENT
SPEC docs/SPEC-TOOLWORK.md:124 rule 4
PKG internal/docs
ASK For rule 4 to hold, `nova-pulse trust` must keep a per-kind record of only the last N cards that decays as new cards arrive, must reset a kind to trial and empty its record (printing `TRUST RESET kind=<kind> area=<area> run_of_fails=3/3` from the harvest that saw the third of R consecutive fails), and must honor `--set paused --reason <text>` by making `cut` cut no card of that kind and making `harvest` re-read the state and answer `ACCEPT ABSTAIN reason=paused` for a card launched before the pause — with the state as one small file per kind under `<queue>/trust/` written by this verb under the queue's lock.
The whole trust verb and record are absent. The tree's own comments say so: internal/swarm/lintheader.go:331 "THE VERB DOES NOT EXIST YET. `nova-pulse trust` is the other half of T06a and belongs to the lane that owns `internal/pulse`", and cmd/nova-swarm/lint.go:342 "`--trust <file>` IS A FIXTURE UNTIL `nova-pulse trust` EXISTS". The only trust code is a downstream consumer — `nova-swarm lint` reads a TRUST fixture file and refuses a paused kind (internal/swarm/lintheader.go:317-321) — not the record itself. Nothing prints `TRUST RESET`, nothing implements decay to the last N cards, no `cut` path checks a paused kind, and no `harvest` path re-reads trust state or emits `ACCEPT ABSTAIN reason=paused`.
Greps run: `grep -rn "TRUST RESET" --include='*.go' .` (no hits); `grep -rn '"TRUST ' --include='*.go' .` (only lintheader.go + its tests, fixture shape); `grep -rn "run_of_fails" --include='*.go' .` (only lintheader.go + tests); `grep -rn "reason=paused\|ABSTAIN" --include='*.go' cmd/nova-pulse/ internal/pulse/ internal/swarm/ cmd/nova-swarm/` (no reason=paused in code); `grep -rn "paused\|trust" internal/pulse/cut.go cmd/nova-pulse/cut_kind.go internal/pulse/harvest.go` (no hits); the `nova-pulse` verb switch in cmd/nova-pulse/main.go:258-303 lists pool/launch/fill/cut/harvest/beat/watch/manager/status/progress/gate/run/triage/sweep/reap/hygiene/fleet/wake/sleep/width and no `trust` case; read internal/swarm/lintheader.go:315-372 and cmd/nova-swarm/lint.go:342-396.
Left owed: none