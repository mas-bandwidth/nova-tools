RESULT tools22-rule-tokens-29 sha=5298f6be12ea — does the code at this base do what docs/SPEC-TOKENS.md rule 29 says?
GAP internal/tokens/package.go:322
SPEC docs/SPEC-TOKENS.md:2338 rule 29
PKG internal/tokens
ASK A publish --batch command must validate every path of the retained batch layout, enforce that shard directory paths match each observation's own friend/bench/day (with `_` and `unallocated` for nulls), refuse unreferenced files/inventories/orphan mappings/coverage/ symlinks/second batch.json/3-way closure failures/malformed envelopes/origin mismatches/whole-package validation failures/unknown schemas (naming the missing validator) as exit 2, refuse --batch combined with --day/--seat/--supersede/--public/--v1-day as exit 2, and keep records/ paths out of v1-day commits and v1-days/ paths out of batch commits.
The library function validatePackage at internal/tokens/package.go:322 implements most directory-and-content validation sub-requirements: shard path origin matching from each observation's own friend/bench/day (line 793-809), `_` and `unallocated` path handling (line 793-801), rejection of unreferenced files (line 402-448), unreferenced inventories (line 930-952), orphan mappings (line 540-562), input coverage/ directory (line 445-446), symlinks (line 405-407), a second batch.json in candidate mode (line 438-441), 3-way mapping closure (line 540-562 and 825-843), malformed observation envelopes (line 732-789), shard path origin mismatch (line 803-809), whole-package count-equation validation (line 954-995), and observation/mapping/coverage envelope schema checks via records.NewValidator().ValidateEnvelope (line 469-485).
GAPS: (1) No `publish` command or `--batch` flag exists in nova-tokens — the pub verb is blocked by cmd/nova-tokens/boundary_test.go:571 TestTheRecordsNamespaceIsNotAVerbUntilItsGateIsDecided, so no exit 2 path exists that invokes this validation. (2) No flag-combination checks for --batch with --day/--seat/--supersede/--public/--v1-day. (3) "naming the missing validator" (unknown schema) is not implemented — internal/records/observation.go:224 lists supported schemas rather than naming the one requested. (4) No commit/push logic exists. (5) No enforcement that records/ paths are absent from v1-day commits or v1-days/ paths from batch commits.
UNGUARDED — no test exercises validatePackage as part of a publish command; only direct unit tests validate the library functions.
GREPS: grep -rn 'fixture.*batch\|batch.*fixture\|FixtureBatch\|fixtureBatch' --include='*.go' internal/tokens/ → empty; grep -rn 'batch\|Batch\|--batch' --include='*.go' internal/tokens/ → found in package.go only (validation logic); grep -rn 'publish' --include='*.go' cmd/nova-tokens/ → 0 hits; ls internal/tokens/ → 48 files including package.go, package_test.go, codex.go, codex_test.go, observation.go (in internal/records/); read full functions: validatePackage (package.go:322-998), ValidateEnvelope (observation.go:156-233), boundary test (boundary_test.go:571-596).
Left owed: Wire the validation into a publish command with --batch flag, add flag-combination refuses, fix "naming the missing validator" to name the schema requested, implement commit/push, and enforce records/v1-days path separation.
git status --short