RESULT tools22-rule-wake-14-L3430 sha=5298f6be12ea
GAP internal/wake/pr.go:379
SPEC docs/SPEC-WAKE.md:3430 rule 14
PKG internal/wake
ASK A watched pull request must be read on one standing `gh api graphql` call, its stored value (comment/review/thread counts, `updated=`, `headRefOid`, newest node) compared whole each tick, and any movement printed as one `WAKE PR` line whose `newest=/by=/review=/url=` name the newest node not set aside by id under `--not-mine` (with `self=` counting set-aside nodes and a set-aside head), a moved count re-fetched as `last:min(d,10)`, a suppressed tick still storing the whole observed value, and `rescan=`/`push=` raised on the stamp-only and push-only movements.
DECIDING LINES internal/wake/pr.go:378-379:
	push := updatedMoved && headMoved && !countMoved
	rescan := updatedMoved && !countMoved && !headMoved
The rule's push case is explicit: "the same tick with that head sha listed in `--not-mine` prints `push=true self=1 rescan=true`". `rescan` here is false whenever `headMoved` (every push), and the only rescue — internal/wake/pr.go:380-383 `if mine != nil && allMine && updatedMoved { rescan = true }` — needs `allMine`, which is only ever set true inside the count-moved fetch (pr.go:347-353) and stays false on a push tick ("only `updatedAt` and `headRefOid` move"). The printed line is therefore `push=true self=1 rescan=false`, and because `rescan=false` skips the clearing at pr.go:385-387, the old `newest=/by=` attribution is kept instead of the rule's `newest=- by=-`. Everything else in rule 14 — newest review `CHANGES_REQUESTED` by `login-a` with `url=`, same state quiet, own comment under `--not-mine` read `self=1` and not a change, a `login-b` comment after it a change, a falling count a change, the `last:min(d,10)` fetch with the `last:2`-shape assertion and the d>10 / `self=10` bound-beats window, the eleven `d=1` ticks each storing the whole value, suppression by id not author on a shared `login=me`, the older-thread reply `rescan=true newest=- self=0` with one graphql call, the every-tick watermark with a moved stamp never silent, `--not-mine` re-read each forge tick, the 20-of-23 owned cap, and the call-count rules — is implemented as written in internal/wake/pr.go:141-429 and internal/wake/source.go:171-183.
UNGUARDED: no test in the tree exercises any `WAKE PR` change line. The rule's own test `TestAnOwnedPullRequestWakesOnAReviewAndNotOnItself` is named at docs/SPEC-WAKE.md:3430 but does not exist anywhere (commit d113f018 "build of SPEC-WAKE draft 9" added events_test.go with tests 13/15/16/17/18 and probe_test.go's 19, but no rule-14 test). No `func Test` in internal/wake or cmd/nova-wake asserts `self=`, `rescan=`, `push=`, `newest=`, `by=` or `review=`; the only WAKE PR assertions are rule 13's cold-poll-quiet and id-file subtests in cmd/nova-wake/events_test.go:176-240. So the GAP above is also invisible to any test.
GREPS grep -rn "WAKE PR" --include='*.go' . ; grep -rn "not-mine|notMine|NotMine" --include='*.go' . ; grep -rn "CHANGES_REQUESTED|self=|rescan=|login-a|login-b" --include='*.go' . ; grep -rn "func Test" --include='*_test.go' . ; grep -rn "func Test" cmd/nova-wake/events_test.go cmd/nova-wake/probe_test.go ; grep -rn "rescan" internal/wake/pr.go ; git log --oneline -15 -- internal/wake/pr.go
Left owed: none
git status --short: