RESULT spec-work-g sha=5298f6be12ea — SPEC-WORK.md has no "Tests this spec demands" section. Read the spec, say which demanded behaviours the tree already tests and which it does not, and draft the issues for the ones it does not.
DONE

<<<SECTION
## Tests this spec demands

These are the lisp acceptance replays of `docs/SPEC-WORK.md` lines 5919-7044 (`## Output
grammar` and `## Acceptance replays`), each named after the spec's own replay name and run by
`make test-lisp` (`./lisp/nova-work/run-tests.sh`) under SBCL. They use in-process kernels, fake
resolvers and fake clocks (`--now`), temp directories, no network and no launcher; each new
replay must be seen red before it is trusted, and parked (deftest-pending) replays are counted
but never run. One behaviour per named replay; the duty-tier replays are listed twice in the
spec under two name-sets (6761-6777 and 6787-6803) and counted once here, under the name-set
that carries tests.

1. `cow-root-partition` — one id is in C or in O and never in both; open+closed equals the counted total; an event hand-written to put one id in both is a rule 18 finding at load and refused at the candidate gate
2. `settle-keeps-id-and-evidence` — a state --to done settles its task: the :id, :children, :acceptance, every evidence event and its five fields read the same before and after, from the closed index, and verify prints the same verdicts over it
3. `settle-moves-no-required-set` — a task finishing leaves its parent's required set, rows=, baseline-rows= and percentage exactly where they were, rule 11 green on the walk; a :cancel moves the set by the delta table's row and that row alone
4. `settle-releases-the-lease` — an item settled while a lease is live reads holder=unowned at once, its :release in the lease log with the settling author and the holder it ended, handoffs printing the whole transition log, no item of C in any who answer
5. `containers-settle-with-their-members` — a feature of three tasks: settling the last task settles the feature and its parent work set in the same envelope and no other container, one OK line and one request id, a crash between the two replaying all-or-none; reopening returns all three to O together; an empty container settles never
6. `reopen-revives` — a :reopen of a done item returns it to O at :todo with id, evidence and generation intact, open= up and closed= down by one; a :reopen of a deferred item writes no :revive
7. `revive-appends-and-counts-latest` — the revived item's closed record and every event under it still in C after the :revive, the :revive's row carrying revived=<rev> and its newest row settles=1; settled a second time, a third row appended, settles=2 on the newest, the cursor naming that newest event revision
8. `index-replayed-after-crash` — a session killed between a :settle and the next clip: the restart's one journal replay puts the item in C's index and out of O's tree before the first ask, open and closed sum to the same total, and a :revive after the settle recovers the item in O and its record in C
9. `remove-settles-only-open-items` — a feature of two leaves, one done in C and one open: node remove settles the open leaf and the feature with :reason removed, leaves the closed leaf's done disposition, evidence and settle stamp untouched, moves closed= by two not three, no rule 18 finding; a fresh-id repeat while still closed appends one typed no-effect receipt changed=0; a removal over a subtree holding a live lease is refused, naming the holder
10. `working-is-a-view` — |W| <= |O| over a set where every item is leased then released; a :doing item with no live lease is disposition=pending; no verb writes W
11. `settle-outside-the-digest` — one state --to done digested by two independent serializers to one value, two :stamp:s and two settle event ids, the retry answered by the original OK line
12. `closed-paged-without-full-load` — a closed listing over a thousand settled items with --max 20 reads twenty rows, prints MORE naming --after, the next page reads the next twenty; parses=0 replays=0 and the whole history never loaded
13. `closed-row-with-archive-absent` — the worked acceptance's ask answering the same four rows with the retention archive file absent, and an ask that reaches for an archived body printing gap=<n> and one QUERY NOTE coverage-gap rather than a shorter list
14. `merged-is-not-distributed` — a fix in C with landed=<sha> and released=- while its release task is open, and released=<version> on the same row once that task settles
15. `branch-and-window-required` — a query --ask with no --branch refused at exit 2 naming the flag; --branch closed with no --from/--to refused the same way; --from under --branch open refused; who, stale and handoffs refused under --branch closed and --branch root
16. `findings-across-c-and-o` — the worked acceptance asserted line by line: four ids, four rows, open=1 closed=3 closed-in=3, no id twice, dispositions distinct, the release task listed pending by the second ask
17. `clip-names-the-index-that-overflowed` — a clip refused with snapshot=, retained=, index= and closed-index= all printed and lower --retain or raise --max-bytes named as the one remedy, the lower --retain then passing; an index page that would pass --page-bytes or --page-records split into a further page by the clip that writes it
18. `default-window-opens-two-days` — a default closed-history listing at early morning, midday and exactly 00:00:00Z opening at most two UTC day partitions and, at midnight, one; no partition older than the window opened for it; a --from reaching back a month opening exactly the days that hold closure records and printing pages= accordingly
19. `busy-day-many-segments` — one day holding many bounded segments read in bounded pages, --max capping the rows, MORE naming --after, the day never read whole
20. `history-grows-startup-does-not` — O, the recent-window volume and the page bounds held fixed while the old history grows: startup resident bytes, segment bytes read, parses, replays and emitted bytes do not move; index pages read bounded by the index depth, never by volume within one depth, no whole-C load, no directory scan, no whole-history dedup
21. `one-revision-publishes-together` — a clip staging its segments, verifying the hashes its manifests name, then committing the snapshot, both index roots and every file they reference in one commit; a kill before the commit leaves the previous root whole; a kill after the commit and before the push leaves work locally durable and unshared, reconciled against the exact remote commit
22. `absent-day-is-not-a-gap` — a day with no manifest inside a complete manifested range answering its rows with gap=0 and no note
23. `missing-segment-is-a-gap` — a segment the committed root names, removed: the listing answering what it can with gap=<n> and one QUERY NOTE coverage-gap, never an empty closed set
24. `as-of-refuses-unavailable-partition` — a state-as-of ask whose day partition cannot be opened refused at exit 1 naming that one partition, never answered from a later row
25. `as-of-reconstructs-settle-revive-settle` — one id settled on day A, revived on day B and settled again on day C: asks ending inside each interval answering that interval's state, the three answers different, the earlier two unchanged, each read one bounded lookup over that id's chain
26. `cursor-pinned-across-a-new-settle` — a paged closed listing with another item settling between two pages: no row missing, no row twice, the cursor's pinned revision honoured, a continuation whose pinned revision can no longer be served refused page expired
27. `activity-and-state-are-two-counts` — an item settled and reopened inside one window printing settles-in=1 revives-in=1 items-in=1 closed-in=0 with open= counting it once, the same window's closed= unchanged by the reopen
28. `dedup-page-unavailable-refuses` — a retry whose dedup page cannot be read refused dedup unavailable, applying nothing, admitted once the page is readable again; a retry across a day rollover, a clip and a successor handoff refused already applied; a reused id with a different payload still refused reused with a different payload
29. `rule-2-unavailable-is-not-green` — a reference into C whose partition cannot be read reported rule 2: unavailable partition=<yyyy-mm-dd> at exit 1, distinct from dangling, and validation never green over history it could not read
30. `wire-integers-are-strings` — an id, a revision, a counter and a token total each above 2^53 crossing the wire and returning unchanged; a frame carrying a JSON number refused; null and an absent key reading alike, an empty string and an empty array reading as values
31. `protocol-version-negotiated-or-refused` — a client offering an unsupported version refused with the supported list named and the connection closed, no request admitted before the handshake, an oversized frame refused with one framed error before the close
32. `pipeline-replies-are-correlated` — pipeline two different queries, a mutation and a long-operation acceptance, deliver their response frames out of order and in fragments: every response reaches only its matching request, the operation id remains distinct; unknown, duplicate and absent response ids close the connection without falsely settling any outstanding request; malformed input with no decodable id receives a null-id refusal; reconnect after a lost mutation response applies no second event
33. `disconnect-is-not-a-rollback` — a client killed after its mutation was journaled: the event stands, the same request id and body returns the recorded disposition, the same id with different arguments refused; rev= and pushed= distinct in every response
34. `no-effect-mutation-is-journaled` — a mutation whose patches are all no-ops: the event id recorded, journal length +1, changed=0 on its OK line, the domain-projection digest unchanged while the event revision advances by one; a fresh-id invocation while still closed is the no-effect case: one typed receipt, changed=0, no second :settle, no scope, membership or counter change
35. `operation-survives-the-client` — a long import returning an operation id, the CLI exiting, the work continuing, the result retrievable by id afterwards, operation wait timing out while leaving the operation running
36. `status-answers-while-io-runs` — status and cancel answered within their bound while a busy capture, export and clip are in flight, queues, staged bytes and retained results bounded, a restart reconciling the operation ids that were pending
37. `cancel-is-a-request-not-an-erasure` — a cancellation acknowledged with its own final disposition, erasing no accepted mutation, reporting an uncertain external effect as uncertain rather than as cancelled
38. `undo-appends-and-preserves` — an undo of a named request appending a typed compensating envelope with its lineage while the original event and every receipt stay exactly where they are
39. `redo-refuses-a-stale-plan` — a redo whose preconditions moved refused atomically, naming what changed, writing nothing, never reached by deleting the undo
40. `dry-run-writes-nothing` — a dry run that validates and projects without mutating: after a green preview at revision R, the --request id is still new to the dedup index, SESSION OK counters unchanged; an accepted mutation moves the revision to R+1; a real apply --expect R refused stale, an apply at R+1 newly validated
41. `undo-refuses-an-external-effect` — an undo over a sent message, a paid execution, a publication and a source deletion refused and reported as an external effect with its own compensating workflow; shared Git history never reset as the undo path
42. `every-field-has-an-owning-verb` — every canonical field mapped to its owning typed mutation or marked derived or immutable, with no generic set-field escape hatch and no parallel alias
43. `open-count-is-read-not-computed` — mutate, then ask |O| repeatedly: zero visits, zero parses, zero replays, the counter equal to an independent full count after a close, a reopen and an import replay; the open-issue and open-leaf counters separate and neither labelled |O|
44. `no-friend-name-in-the-tool` — the binary, its defaults and its shipped fixtures carrying no friend, bench, repository or house name; every identity arriving as configuration (the spec itself says this is not a test of this document)
45. `roles-are-configured-not-inferred` — a role read from CONFIG and never from the underlying model; a model capability never cancelling an agreed limit
46. `reserved-role-is-not-spent-on-routine-work` — an essential-security-only reserved role, a specialised different-perspective reserved-plan role and an agreed participation each expressible, and the reserved ones not spent on routine work
47. `dispatch-ack-and-ownership-are-three` — dispatch, delivery, acknowledgement and accepted ownership distinguished; a pending offer reserving only declared capacity; a timeout alone launching no duplicate while the old worker may run; a return reconciling before new dispatch
48. `requested-model-is-not-observed-model` — unknown staying unknown, concurrent attempts keeping separate model and usage attribution, a friend's usual model never standing as proof of a delegated task's executor
49. `a-retry-does-not-overwrite-its-attempt` — a retry never overwriting its attempt's model and usage attribution
50. `silence-is-a-ping-not-a-verdict` — the configured silence threshold triggering one bounded ping, a nonresponsive capacity marked unavailable with reason unconfirmed and no claim of sleep or exhausted credit, a failed probe read as unresolved delivery
51. `explicit-rest-is-not-pinged` — explicit rest respected, not pinged
52. `return-reconciles-before-dispatch` — a return reconciling before new dispatch
53. `four-facts-four-verbs` — an admitted offer with declared free slots writing :effect :dispatched, a pending-offer index entry and a reservation, and leaving node state, W, the lease index, attempts, evidence and completion unchanged
54. `offer-writes-intent-and-a-reservation` — acknowledge --stage received writing delivery only; nothing inferred from anything
55. `a-receipt-needs-a-verifier` — a copied note and an --as <recipient> with no verifier result refused with no canonical write
56. `staged-admission-refuses` — a verifier or payload reader that returns after a conflicting revision, or fails validation, writing no reservation, receipt, lease or W change while session status answers inside its bound
57. `accepted-creates-one-lease-or-binds` — an accepted receipt after received creating exactly one :lease and one W entry, or binding a second attempt to the same holder's lease with its deadline unchanged, converting and never doubling capacity, an absent observed model recorded unknown
58. `no-shadow-lease-across-holders` — a cross-holder offer or accepted reply refused or retained late and creating no lease
59. `until-is-overdue-not-released` — at --until and at lease expiry no duplicate launch and no stopped or completed claim, the reservation and any uncertain execution retained until reconciled
60. `late-and-duplicate-receipts-are-retained` — a late accept after a decline, a replacement, an expiry or a generation change retained :late, reviving no lease and overwriting no successor; the same request replaying its success, the same verified receipt under a new request consuming no capacity, conflicting bytes for one receipt id refused
61. `stop-is-a-hold-not-a-cancel` — execution stop --node writing a hold and directives and no transition, goal show still printing stop=none
62. `one-stop-note-cannot-cancel-two-attempts` — state --to cancel-requested then an event --kind cancel whose evidence covers one of two live attempts refused, the same with both covered admitted
63. `hold-survives-a-crash` — a crash after the hold is durable and before capture or send recovering the same hold and target identities with no duplicate launch
64. `capture-survives-clip` — a clip completing between the anchor and the manifest, the pin resolving the same revision and span, an unrepresentable pin refusing admission before EXECUTION OK
65. `no-dispatch-slips-past-a-hold` — an offer prepared before a pause refused at the last send; a launch, a correction and a move raced against a scope pause all held
66. `held-acceptance-converts-nothing` — an acceptance under a hold retained :accepted-held with no lease, launch or release until reconciled, and a lift of the hold converting nothing
67. `reconcile-preserves-contradiction` — live, expired, pending, paused, unsupported and unreachable targets captured exactly through the indexes in bounded pages; duplicate, delayed and out-of-order receipts and a forged source inferring no ownership, release, cancellation or completion; two contradictory observations kept unresolved; a stop report carrying no synthesised zero usage; two attempts on one task stopped by two receipts, usage retained apart, the task counted once in W; the holder-only release rule unbypassed by a confirmed exit
68. `resume-is-two-actions` — release-hold lifting only its control's hold with an overlapping hold still effective and no process resumed; resume-workers refusing an unsupported capability before sending and holding until a running observation, a delivery receipt alone releasing nothing
69. `correct-is-a-linked-segment` — one envelope writing the hold, the :correct and the instruction binding, a retry bumping the generation once; a worker's old-generation usage kept as its own segment
70. `bare-correct-refuses-under-execution` — the bare correct refused by name while an attempt is live and admitted once none is
71. `endpoint-is-local-and-private` — the session's directory created 0700 and its socket 0600, both owned by the account that runs it, a pre-existing directory or socket with wider modes refused rather than reused, the Windows named pipe created with FILE_FLAG_FIRST_PIPE_INSTANCE, and no listener bound to any network address
72. `four-capability-groups-and-three-fields` — child agents, swarms, local models and one-shots each expressible as a capability group with a stable id, its source, its last-verified stamp, its availability and its constraints; declared support, verified runtime and current free capacity kept as three fields that never collapse into one
73. `unchanged-config-is-one-bounded-answer` — the config exchange bounded, validated and atomic, with no roster and no prose repeated per poll and no secret in a manifest
74. `an-invalid-delta-leaves-the-old-config` — an invalid config delta leaving the old config intact
75. `a-partial-manifest-is-refused` — a partial manifest refused
76. `pricing-is-pinned-by-revision` — an old estimate reproducible after a rate change
77. `unknown-price-is-not-zero` — a missing dimension unknown, never zero
78. `subscription-is-not-free-reference-cost` — a subscription not read as free reference cost; the three cost values kept separately labelled
79. `local-tokens-cost-zero-api` — local tokens cost zero API
80. `roadmap-outlives-its-work` — an epic completed, clipped, restarted and advanced past 24 hours, then its roadmap listed and opened, rendering identical historical rows and retrieving the exact code and test receipts without loading all of C
81. `roadmap-opened-after-the-window` — completed rows still in the table and remaining only an explicit filter; a referenced task reopened and a new sub-feature added, the affected rollups updated without dropping a completed member and without double-counting a shared prerequisite, the summary and expanded-row views exercised over the same ids
82. `historic-tick-survives-a-source-change` — a changed source or criterion preserving the historic tick at its pinned revision while the current view requires re-verification
83. `regression-opens-repair-work` — a confirmed regression creating linked open repair work
84. `unrelated-receipts-stay-reusable` — unrelated receipts untouched
85. `chat-and-file-render-are-byte-identical` — one projection and revision rendering the same bytes to chat and to a marker region, every other byte preserved
86. `render-refuses-a-target-outside-its-roots` — a missing, duplicate or reversed marker pair refused, and a target outside the configured permitted roots refused rather than guessed
87. `restore-is-isolated-and-dispatches-nothing` — a restore taking no ownership, reanimating no assignment and replaying no message
88. `a-savepoint-is-not-a-shared-backup` — savepoint age, the local and the shared revisions, unshared work and failed backups all readable, a savepoint never printed where a checkpoint was asked for
89. `compaction-keeps-the-last-copy` — compaction never removing the only recoverable copy
90. `new-verbs-have-a-kind-and-a-field-order` — undo, redo, friend, model, observe and config --intake each writing an event of its own kind, with every field of its list written in order and absent ones written (:absent), :node written (:absent) on all six, and a second build serializing each one to the same bytes
91. `new-verbs-retry-to-one-event` — each of the six replayed twice under one request id yielding one event: the second call answered with the original OK line, applying nothing, across a clip and across a successor handoff; the same id with a changed payload refused reused with a different payload; each OK line carrying the subject its kind names and never an empty node=
92. `absent-empty-and-null-are-three-spellings` — one request giving no :members, one giving an empty list and one giving a wire null digesting to three values of which the first and the third are equal and the second differs; the same three surviving a wire round trip unchanged
93. `clip-is-one-long-operation` — clip returning OPERATION OK id= op=clip and exiting, the transport continuing, operation wait --id printing the CLIP OK line with that operation=<id> and its pushed=, a raced transport printing CLIP RACED through the same wait, and session stop printing the same CLIP OK first and then SESSION OK by waiting on its own operation inside --git-timeout
94. `undo-names-its-reversible-set` — every row of the reversible-verb table exercised: each reversible verb undone by the envelope the table names, each refused verb refused not reversible here naming itself, an undo over event --kind cancel and over node remove refused because both dispositions are terminal, and no undo reaching a terminal state by any path
95. `add-field-order-is-complete` — a node add with every one of the twelve fields given, one with each absent, one with --links-empty and one with --clear-links, digested by two serializers to one value each and to four distinct values; a fixture in the pre-fold order refused schema revision unsupported at load, never read as the new shape
96. `repo-only-at-the-root` — --repo with --under-root open --type work-set accepted and unique; the same --repo again refused repo held by <id>; --repo under a parent refused repo outside root; node edit and node move unable to change it
97. `roadmap-has-one-creator` — node add --type roadmap exit 2 naming roadmap create; roadmap create writing one node and one view in one envelope, a crash between them replaying all-or-none; no second alias
98. `metadata-patches-preserve-intent` — keep, clear, set-empty, set-false and set-value on each of the five fields round-tripping and digesting distinctly; an all-keep, a malformed tag and a wrong type refused with no event and no counter moved; --version set on a :feature refused version on a feature and its keep accepted
99. `edit-is-atomic-and-replayable` — a bad one-of-five patch writing nothing; an accepted mixed edit moving only its named fields and the category index; the same request id retried answered by its original NODE OK; a changed payload refused; an equal-value edit the no-effect receipt, changed=0, rev up by one
100. `edit-undo-preserves-later-work` — an edit undone restores :before; the same undo after an intervening edit refused conflict, both events standing
101. `edit-never-fetches-a-link` — a link that is a live URL to a counting endpoint added, edited and rendered with zero requests observed; a link holding NUL refused bad link; a refusal on a private node printing no value
102. `move-keeps-every-count` — a required subtree moved between two features: the source's and destination's required sets and open counts move by the subtree, the common ancestor's net count is stable, |O|, |C|, W and every task state unchanged, rule 11 green on the walk, and no whole-set scan (visits asserted)
103. `move-same-parent-is-a-receipt` — --from equal to --under and true: the structure event alone, changed=0, sibling order unchanged; a lost reply retried yields one envelope; a different payload under the id refused; every refusal leaves both parents unchanged
104. `move-refuses-by-name` — a wrong --from, a destination inside the subtree, a root container, a repository root, a shared container, another repository, and a roadmap as either parent, each refused with its named reason and the identity, nothing written
105. `move-keeps-the-lease` — a working subtree moved with its effective :responsible unchanged keeps the same lease, attempt and usage; a move that would change it over an unreconciled attempt refused active context change naming the ids; a move under a public parent from a private one refused privacy reduction, the reverse admitted and the public render losing the rows
106. `move-updates-every-roadmap-scope` — a row referenced by two roadmaps outside both parent chains and one unrelated roadmap: both referencing scope revisions advance in the envelope, the unrelated one stays, a failed acceptance moves none, historical renders keep the old captured scope, an intervening affected-roadmap mutation makes undo conflict
107. `move-undo-refuses-a-reorder` — undo after a sibling reorder, a further reparent or a privacy change refused conflict and guessing no position; undo otherwise restoring the exact before order and required sets with fresh scope revisions, the old numbers unwritten; a kill around acceptance and during clip exposing neither two parents nor none
108. `axisless-history` — two ordered rows added by roadmap row, one finished, the state exported and loaded, the view reopened past the default window: both rows and their evidence present, the denominator not reduced by completion; a row retired records a scope movement, keeps its node, and the prior view reconstructs at its captured revision
109. `matrix-retirement` — axis --remove of a first-axis row and then of another axis's member: only the selected coordinates retired and recoverable, no task cancelled, an unknown member refused, a layout change on a populated roadmap refused layout populated with no partial write, and a matrix never flattened without explicit selections
110. `configure-no-effect-and-undo-conflict` — an equal-value configure, its reply lost, a later edit, then the retry: the original receipt returned and the later value kept; undo restoring an ordered preimage only while its guards match
111. `completed-view-mutation` — metadata, projection and render on a settled roadmap reviving nothing; an outstanding member added applying the atomic revival rule so no settled container silently holds open required work; counts and indexes checked by the reference fold after each step
112. `render-artifact-is-bounded` — ordinary replies and a --chat artifact interleaved in one correlated batch, request ids, byte length and hash verified; a corrupt or oversized artifact a bounded refusal and never partial Markdown; --check creating no target, no receipt claiming a write, no commit and no push
113. `a-root-id-grants-nothing` — a stored permitted root with no --render-root mapping refusing file mode while --chat renders; an escaping path, a symlink escape and a target identity other than the mapping's refused; the cooperative lock exercised and its external-editor limit retained
114. `priority-orders-only-the-eligible` — a blocked rank-0 task stays blocked with its reason and resolver while a rank-9 ready sibling is first among the eligible; --order priority under done exit 2; capacity loss, approval withdrawal, a dependency change or a hold rechecked before ranking and starting or interrupting nothing
115. `priority-inherits-and-clears` — a root :subtree rank changing ready order with no lease, attempt, state, O, C, W, counter, baseline or roadmap moved; a child's :self overriding it; a clear revealing the parent; settle and reopen keeping the slots; a move re-reading inheritance with no cloned event
116. `rank-2-precedes-10` — ranks compared as integers, and equal and default rows ordered by id across a restart, a handoff, a cursor continuation and skewed clocks; a first unseen filter O(k log k), later pages from the pinned order, a subtree invalidation touching no unrelated scope and no C
117. `priority-undo-is-history-not-value` — a same-value set and a clear of an absent slot each the no-effect receipt; set 2, set 9, set 2, then undo of the first refused although the value matches
118. `priority-grants-nothing` — with priority set on every node, who unchanged, no lease written, no worker selected, no approval bypassed
119. `state-export-describes-exactly-r` — capture R while R+1 is accepted and the bytes describe R; an exact-snapshot export with B equal to R and an absent end, and one with B below R over a multi-record prefix across a rotation; an absent end below R, a missing or swapped record, a wrong end hash or revision and a cut inside an envelope each refused, and a present later tail never replayed
120. `state-export-refuses-a-gap` — a missing mandatory member, a changed digest, a dangling internal reference, a path escape, a symlink, an output overrun and a corrupt S-expression each refused with no valid load; a historical export whose resolver observations are gone refused with a named proof gap and never given current ones; --closed-history range over [from, to) declaring its omissions while keeping closure, all reaching C past the resident window and a fresh load reproducing its proof
121. `state-export-is-one-long-operation` — an export blocked on archive I/O acknowledging its operation at once, status, cancel and an unrelated mutation responsive under it, and wait returning the same operation and captured revision after publication or refusal; an export inside an atomic batch refused by entry id
122. `state-export-pin-survives-clip` — capture R, a clip and a retention pass at R+1 during the copy, then exactly R completed or a recovery gap named; no pinned member reclaimed, no current bytes substituted
123. `state-export-disconnect-and-cancel` — a lost client, a restart and a cancellation around the no-replace publication keeping one operation and one output identity, no duplicate directory, no claim to reverse a published one; an existing destination refused; a staged manifest before the commit not published
124. `state-load-is-isolated` — an instrumented export and load with no ownership change, no dispatch, no replay, no merge, no resolver run, no network and no repository write, only the declared exclusive paths written; the loaded snapshot answering query --snapshot and refused as a --session by every mutation, replay, clip and handoff verb; a re-export comparing equal in every field, id, Unicode, empty and absent value, role, CONFIG, ACTIVE, O, C, roadmap and accounting record
125. `fenced-export-can-finish` — in a fenced session an export started, its status and terminal line read by its id, and separately an unfinished one cancelled with publication reconciled; an unknown id, a non-export id, operation list and every canonical write refused fenced; no second owner and no mutation authority created
126. `goal-crosses-harness` — G a :doing leaf at revision r; harness A as coordinator C: goal set --goal G --expect r prints GOAL OK rev=r+1, writes a (:coordinator "C") note with a :deny on a made-up model for :coding, goal update --stop on G; harness B another build shows --as C against live and clipped snapshot: both print goal=G, a rev= at or after every write of A's, stop=requested not cancelled, the same note id and constraint row byte for byte; B's update --progress refused stop requested, nothing written; A writes event --kind cancel --evidence; B's next show prints stop=cancelled
127. `goal-stale-update-refuses` — A and B both show at revision r; A writes update --progress with the evidence triple on G, a :doing leaf, r+1; B's update --progress --expect r refused GOAL FAIL expect=r current=r+1: stale, snapshot unchanged, B's next show prints A's evidence row; A then update --stop --expect r+1 (r+2); B's update --progress --expect r+1 refused stale, show prints stop=requested; B's update --progress --expect r+2 refused stop requested; stop=requested stands until a :cancel with evidence or state --to doing by id; A goal set to a node on the closed branch refused disposition=done whatever --expect says
128. `goal-stop-is-a-request-not-evidence` — G :doing at r; goal update --stop --reason --expect r prints GOAL OK change=stop kind=transition rev=r+1, the event a :transition :to :cancel-requested carrying :reason and no :evidence; check has no finding; state --to cancel-requested on a sibling writes an event of the same kind and fields; state --to doing by id admitted (withdrawal), show prints stop=none; stopped again, then event --kind cancel --evidence, show prints stop=cancelled terminal, goal set refused disposition=cancelled; goal update --stop on a :review node refused no edge, as state --to cancel-requested is there, and on a :done node the same
129. `goal-update-writes-only-existing-kinds` — every goal update form written, then the journal read: each event a :transition or an :evidence with exactly the field list of its kind, on the goal node of the scope and no other node; --progress <text> alone on a :todo node writes :to :doing with the text as :reason (the same event state --to doing --reason writes) and on a :doing node refused no edge, nothing written; --progress with the evidence triple on a :doing node writes the :evidence event evidence would; goal set and goal set --clear each write one :goal event with :node (:absent); accept --add moves its scope revision and goal update never does; a retried set with the same --request id and payload returns the same event id once
130. `goal-expect-is-required` — goal set and goal update without --expect exit 2 naming the flag, nothing written; with --expect at the current local revision admitted; with --expect one behind refused stale at exit 1 with the current value printed; --dry-run with the stale expectation prints the same refusal and writes no event, no journal revision and no dedup entry; a show never takes --expect and answers at the revision it prints
131. `applicable-cap-never-hides-a-deny` — N active notes, N > --max, the only :deny in the note that sorts last (Delegation's applicable); applicable --max 1 --candidate <role>/<model> prints excluded <that id> and NOTES MORE; goal show --max 1 prints the same constraint row before any cut row, then GOAL MORE; with the notes index unloadable, both print FAIL and neither prints eligible nor any row
132. `savepoint-cut-never-splits-an-envelope` — a two-event request accepted and a savepoint after it: both events represented once in the image and a retry answered the byte-identical original lines; a savepoint whose cut would fall between the two events refused cut inside an envelope and no image published
133. `crash-after-append-recovers-the-reply-once` — a crash after the durable append and before the reply: the envelope replayed once from the record, the same request and payload answered with the original OK, the retry mutating nothing; the same id with a changed payload refused reused with a different payload after the restore as before it
134. `rotation-keeps-one-journal` — a clip rotating the journal after a savepoint: the new segment's header naming the same journal id and the copied boundary record with its sequence and hashes, the chain continuing, the savepoint's cut and every disposition it needs reachable without a scan of old segments, session export --journal writing the same bundle from either file
135. `torn-tail-is-diagnosed-not-truncated` — a partial frame at the end of the journal, a flipped bit inside a frame and a wrong header each refused with the file bit for bit as it was, the first reported recovery-gap kind=torn-tail, the second kind=corrupt-record and the third journal mismatch, none rounded to another, every mutation refused journal uncertain after a failed append until the tail is read
136. `replay-mints-nothing` — a replay of five envelopes reconstructing the exact root digest, the closed rows and the next revision with no fresh event id, no clock read and no verb run; a replay failing midway leaving its target untouched
137. `savepoint-write-failure-keeps-the-previous` — the image write, the manifest publication and the sync each failed in turn: the previous verified savepoint restores, and savepoint list prints the attempt verdict=failed
138. `copied-journal-grants-nothing` — a journal and its savepoint copied to another bench: savepoint restore there inspects in isolation, takes no ownership and dispatches nothing, and a session start over the copy is refused by the fencing rules, the journal id notwithstanding
139. `reply-retired-only-under-verified-coverage` — a request whose events lie before the clip boundary: its retry answered already applied only once the committed snapshot, its retained events and the dedup root the boundary record names are verified reachable; with the push failed or the root unreadable, the original OK still answered from the retained disposition or recovery-gap kind=coverage-unverified printed, and never a reply invented or deleted
140. `indivisible-record-refused-before-ack` — one key with its locator that no page under --page-bytes could hold refused indivisible at exit 2 with nothing journaled, while a request whose repeatable evidence would pass the bound is admitted and chunked behind a detail root; session start --page-records 2 refused at exit 2 naming the flag
141. `days-merge-by-revision-never-concatenate` — a closure backdated by --now into an earlier day: its row in that day, and a --from/--to over both days printing rows in revision order across the day boundary; the same day clipped in one batch and in ten yielding identical leaves
142. `page-budget-is-not-max` — a filtered historical ask whose filter rejects every row read: QUERY MORE shown=0 pages=<n> at the budget, the whole history never scanned, --max untouched by it, the continuation answering from the same captured root
143. `overlay-is-bounded-and-rebuilt` — a recovery replaying a thousand settles into overlay pages under --index-cache, an eviction under load losing no row, the next clip writing them into the pages, and no query replaying the journal
144. `duty-tier-executes-the-policy` — a resident session (E02) given an approved finite policy record from C, :by on every rule, executes it with the cheapest qualified model or none, authors no policy of its own, and escalates every judgment it cannot make; a policy rule with no :by is refused at load
145. `escalation-carries-rule-default-age` — an escalation row carries the policy rule that could not decide it, the default that fires on silence, and its age; stale reads the three and reassigns nothing
146. `wait-table-four-presence-columns` — the per-harness wait table carries the four presence facts -- process alive, beat written, delivery handled, parent woke -- and a harness with the fourth unproven holds no resident session, only a duty session driven by notes
147. `quiet-time-calls-nothing` — a resident or duty session with nothing changed makes no model call and sends no note; state is published mechanically; the cost of one event is the measured spend of the one call that event caused
148. `cost-per-accepted-decision` — the coordination measure is cost per accepted decision across tiers, with wrong or missed decisions and recovery latency as gates; the decisions-per-token sentence of Decision packets is replaced
149. `single-writer-kernel-total-order` — two concurrent clients' commands to the one kernel land in one total order; the journal shows the sequence numbers in that order; a mutation outside the command loop is a defect (the validator rule)
150. `machine-is-config-and-never-a-work-tree-node` — nova-work machine --register m-a1 --name studio --owner glenn --connect profile:studio --role build --role test --permit go-test --limit cores=16 --fact os=macos --declared-by glenn --reason "studio online" prints MACHINE OK id= request= machine=m-a1 rev= pushed= changed= emitted=; |O|, rows=, every roadmap and every count unchanged, the event's :node (:absent), the record a :kind :machine member of the fleet section of CONFIG, and no verb can settle it, :to :done it or make it completion evidence
151. `allocation-binds-machine-slot-generation` — nova-work take --session <path> --machine m-a1 --node schema/cpp/refuse-newer --slots 1 --offer <offer-id> --attempt <attempt-id> --generation <n> --request-ref <opaque-id> --batch <request-id> prints ALLOC OK id= request= machine=m-a1 allocation=<id> slot=1 node=... batch= offer= attempt= machine-generation=<n> allocation-generation=<g> rev= pushed= changed= emitted=; the allocation is ACTIVE data referencing the machine's CONFIG identity and revision, the (offer, attempt) is the offer section's reservation key, admission happens before preparation and both capacity constraints validate atomically in one binding, and the core-pin rule stays a declared :limits constraint and is not in the allocation
152. `allocation-take-is-atomic-and-idempotent` — take with --slots 2 prints ALLOC OK allocation=<id> slots=2; the retry under the same request id prints the original ALLOC OK line and applies nothing, a changed payload refused reused with a different payload, a take for more than the declared :concurrent refused whole ALLOC FAIL machine=m-a1 slots=2 holder=<name>: capacity at exit 1 with no partial grant; heartbeat prints ALLOC HEARTBEAT OK allocation= machine= machine-generation= allocation-generation=; a heartbeat with a stale allocation or machine generation prints ALLOC FAIL allocation=<id>: stale token; release prints ALLOC RELEASE OK allocation= machine= slot= freed=true and frees exactly that slot; list --machine m-a1 prints one ALLOC ROW per live allocation
153. `expiry-marks-suspect-reuse-needs-fencing` — an allocation past its deadline reads suspect: a renewal and a take with the stale token refused ALLOC FAIL machine=m-a1: suspect since=<stamp> at exit 1, the uncertain ACTIVE capacity retained and never cleared by the expiry; only a verified stop observation, a bound not-started or machine-side fencing releases the slot, and until one of those the refusal is ALLOC FAIL machine=m-a1: not fenced at exit 1
154. `probe-records-observed-active-and-touches-no-config` — nova-work probe --session <path> --machine m-a1 --slot 1 --source <pointer> prints PROBE OK machine=m-a1 slot=1 fact=observed at=<stamp> source=<pointer>, and an unestablished fact prints fact=absent; query --ask fleet prints the same declared :facts with their declared-by and declared-at before and after, no :limits, no :facts, no :connect and no :roles change, the profile never resolved, and no credential in any record or clip
155. `one-allocator-per-machine-aliases-share-nested-conserve` — a second allocator for one machine refused ALLOC FAIL machine=m-a1: allocator held at exit 1; an allocation taken through one alias of a host reads as the same allocation under the other alias and counts once; an allocation nested under another allocation's scope draws from the same declared capacity, never an additional slot, ALLOC ROW listing each live allocation once
156. `release-one-allocation-spares-the-other` — machine m-a1 with :concurrent 2 holds alloc-a (slot 1, node N1) and alloc-b (slot 2, node N2); release --allocation alloc-a --generation <n> prints ALLOC RELEASE OK allocation=alloc-a machine=m-a1 slot=1 freed=true and list --machine m-a1 then shows exactly one ALLOC ROW for alloc-b with slot=2; the released slot 1 is free for a new take --slots 1 while alloc-b continues
157. `stale-allocation-id-refused-by-name` — after alloc-a is released, a heartbeat or release using --allocation alloc-a --generation <n> refused ALLOC FAIL machine=m-a1 allocation=alloc-a: stale token at exit 1, naming the allocation id that no longer matches any live allocation; a take using the released allocation's generation against a changed machine configuration refused ALLOC FAIL machine=m-a1 slots=1 holder=<n>: capacity at exit 1 by the stale machine generation check
158. `preparation-interrupted-before-launch` — take allocates slot 1 on m-a1 to node N1, allocation alloc-c; preparation (clone/scp) interrupted before the model launches; a second session's take --machine m-a1 --slots 1 for node N2 refused ALLOC FAIL machine=m-a1 slots=1 holder=<N1>: capacity because alloc-c still holds the slot; only after reconciliation (a verified stop observation, a not-started rejection, or machine-side fencing) frees alloc-c does the slot become available, and no second session reuses the capacity until then
159. `concurrent-slots-refuse-third-job` — machine m-a1 with :cores 16 and :concurrent 2 holds two allocations each consuming one slot; a third take --machine m-a1 --slots 1 --node N3 --generation <n> refused ALLOC FAIL machine=m-a1 slots=1 holder=<name>: capacity at exit 1 even though fourteen cores sit idle, because slots are bounded by declared concurrency and cores are a separate constraint validated independently
160. `route-config-lists-key-by-path-never-value` — nova-work route --register anthropic/claude-3.7-sonnet --provider anthropic --endpoint https://api.example.com --key-location (:env "ANTHROPIC_KEY") --plan metered --cost-per-mtok 15 --capabilities (:text yes :code yes :tool-calls yes) --owner glenn --reason "team route" prints ROUTE OK id= request= route= rev= pushed= changed= emitted=; query --ask routes lists the member with every field but the key -- the key is a path or an env name and no value is ever printed, stored or echoed; a :key-location that is neither refused credential in record with the value never echoed; |O|, rows= and every count unchanged, the event's :node (:absent), and the record a :kind :route member of the routes section of CONFIG
161. `routing-picks-flat-before-metered` — a card class whose eligible routes are one metered, one free and one flat projects flat first, then free, then metered; two metered routes order by :cost-per-mtok ascending, and a tie by bytewise stable id; a route listed twice in the class's list is two shares of its routing; the projection is derived at the named scope revision and never a stored list
162. `unprobed-route-carries-no-card` — a route with no passing probe -- no probe record, or a probe whose :pass is false, or a live :benched-until -- is absent from the generated cards of its class, never present with a warning, and the card builder refuses to cut a card for it; once a probe passes and the bench clears, the route carries cards again
163. `three-abstains-bench-until-probe` — a route whose probe thrice in a row answers pass=absent reads benched, its :benched-until set and its cards carried by none, and a further probe before that date changes nothing; the next probe that passes clears the bench and the route carries cards again
164. `prompt-profile-expired-shows-on-the-status-line` — a manager whose profile expired yesterday: session status prints SESSION OK profile=sonnet profile-state=stale profile-expired=2026-09-15 profile-owner=<name>; a manager with no profile prints profile=sonnet profile-state=absent; a current one prints profile=sonnet profile-state=current profile-expires=<stamp>; a manager session names its profile on that same line; only the coordinator's profile verb writes a profile, every edit a versioned record carrying :by and its own revision
165. `a-done-need-on-unverified-evidence-admits-nothing` — N settled done on a run: pointer the fake resolver has not yet answered: query --ask ready --branch open --node D prints D's row ready=false reason=need-unverified need=N unmet=1, and take --node D prints LEASE FAIL node=D unmet=1: unmet need N need-unverified at exit 1 with the journal, W and the lease index unchanged; after verify --node N against a resolver answering holds, and no other command, the same ask prints ready=true and the same take prints LEASE OK; the same run with the resolver answering absent leaves both refusals standing
166. `stale-evidence-does-not-unmeet-a-need` — N met, then source --to <sha> on N's repository work set: N's evidence reads stale and N counts unknown in its rollup, while D's row still prints ready=true unmet=0 and take --node D is admitted
167. `every-unmet-need-has-one-reason` — five fixtures, one per row of rule 2's table, each printing its one token, need= and the resolver that row names; over a cancelled need, dep --remove of the edge and then ready=true with no other command; over a superseded need, the remove and a dep --add of its :superseded-by; two unmet needs printing the first in :deps order and unmet=2; no fixture moving D's state=
168. `a-container-need-is-met-with-its-members` — a need that is a :feature of two tasks: one done and verified and one open, need-open; both done and one on unverified evidence, need-unverified; both verified, met; an empty container never met
169. `every-admission-verb-refuses-an-unmet-need` — over D with N open and no lease, offer or allocation on D: take --node, take --node --for <name>, offer, state --to doing, goal update --progress with D the scope's goal, take --machine naming an offer that does not exist, and task packet; and over a D that was leased, offered and received while N was met and whose N was then reopened: release --handed, reassign, acknowledge --stage accepted, take --machine on the real offer, task packet, and undo of D's earlier release. Each exits 1 with its own FAIL line ending unmet need N <reason> -- take --machine and task packet printing that and not unknown offer or no lease, by rule 3's order -- STATE FAIL carrying no rule number, and no lease, offer, reservation, allocation, W entry, transition, compensating envelope or packet file written; the same calls under --dry-run print the same refusals; with N met, each prints its OK; with a scheduling hold over D as well, offer prints held by control <id> and an acceptance is retained :accepted-held, which execution reconcile leaves unconverted while N is unmet; a take --node of an id the session does not hold prints that verb's no-such-node refusal and no unmet need
170. `a-reopened-need-under-a-doing-dependent-leaves-the-set-green` — D :doing on N met; event --kind reopen on N; then check, a clip and a fresh session start on that clip: each prints findings=0 and no WORK FAIL line names D or rule 10, WORK OK prints needs-broken=1, and an unrelated evidence on another node is admitted after each
171. `a-verb-that-can-admit-declares-its-needs-gate` — the schema file's coverage test, over a fixture schema: a verb entry whose event kinds include :lease and which carries no needs-gate fails the test naming the verb; the same entry with needs-gate: refuses passes; needs-gate: exempt with no reason fails; an acknowledge entry marked refuses whose needs-gate-forms omits --stage accepted fails; and the shipped file passes with every verb of rule 3's list marked refuses and execution reconcile alone marked withholds
172. `a-removed-or-corrected-need-is-unmet` — R removed by node remove while no edge names it, then dep --add R on D: admitted, DEP OK met=false, and D's row prints reason=need-closed-unaccepted need=R; N met, then correct --node N: D's row prints reason=need-unverified, and after N's reopen, new evidence, verify and done at the new generation it prints unmet=0
173. `an-attested-only-need-is-met-by-its-attestation-and-by-nothing-less` — N with one :attested criterion, done on an attest whose --result pointer the resolver has not answered: need-unverified; the pointer verified: met; after a correct, unmet again; the same attest typed under another --as meets it just the same
174. `ready-true-implies-needs-met-and-not-the-reverse` — D needs-met and leased by another name: its ready row prints ready=false unmet=0 with an ownership reason and take --node D prints LEASE FAIL: held; across the whole fixture no row prints ready=true beside an unmet= above zero
175. `needs-met-is-read-not-released` — N's evidence verified in the cache, then state --to done on N: after that OK line and with no further command the row of D, unowned, prints unmet=0 ready=true, and the journal holds no event whose :node is D; the same for a dep --remove of D's last unmet edge
176. `reverting-a-need-flags-an-engaged-dependent-and-kills-nothing` — D leased, :doing and heartbeating on N met; E, a second dependent of N, settled in C; F a dependent of E. event --kind reopen on N: D's row prints reason=need-reverted needs-broken=true, check prints WORK OK needs-broken=2 at exit 0 with no WORK FAIL <id> line, D's lease, attempt and next heartbeat stand, task packet for D is refused by rule 3, E stays in C with every count of its scope unchanged, and F's row prints needs-broken=false; N settled and verified again, D and E read needs-broken=false with no command naming either
177. `an-edge-added-under-work-flags-and-every-way-past-a-need-is-recorded` — dep --add M of an open M onto leased D prints DEP OK change=add need=M met=false unmet=1 needs-broken=true and stops nothing; onto an unengaged :todo node it prints needs-broken=false; dep --remove N, take --node D, dep --add N with N still open leaves three events with their authors, the lease standing and needs-broken=true; dep --add D on D and an edge closing a two-node cycle are each refused rule 3, an id naming nothing rule 2: dangling, each exit 1 with nothing written; a need under another repository work set of the same O is admitted; over a container need with one unverified member, node require --to false on that member prints its :require scope event, moves the container's required= by one and leaves the dependent unmet=0
178. `report-records-and-changes-nothing` — report --act other --subject machine:<id> --what ... --acted-at <t> --instead-of - --reason ... under --now <t + 90s> prints REPORT OK subject=machine:<id> act=other instead-of=- acted-at=<t> lag=90s unmet=- need=- holder=-; the journal gains one :report event with :node (:absent) and its six fields in order; |O|, W, every lease, every count, every roadmap and the machine's member unchanged; a retry of the request id prints the original line, a changed payload under it refused reused with a different payload, an undo of it refused not reversible here, and two serializers digest it to one value with :unmet and :need outside the digest
179. `report-refuses-by-name` — each of the six flags missing, an --act outside the three, a --subject with no : or of an unknown kind, and a whitespace-only --what, --reason, --instead-of and external: text: each exit 2 on one line ending run: nova-work help; a subject naming no node, machine, friend, route or offer, an --as that is no friend, an --acted-at ahead of the clock past --skew, and a stale --expect: each REPORT FAIL at exit 1; in every case no event, no journal record and no dedup entry
180. `a-hand-launch-is-refused-when-asked-and-recorded-when-told` — over D with N open: take --node D --dry-run and take --node D both refuse at exit 1 and no flag changes either; report --act launched --subject node:D prints REPORT OK unmet=1 need=N holder=unowned, writes no lease and no transition, leaves who unchanged, and D's row then prints needs-broken=true; the next take --node D still refuses; N met, D's row prints needs-broken=false and take is admitted. The same report over a needs-met node held by another name prints unmet=0 need=- holder=<name>
181. `a-report-of-a-stop-releases-nothing` — report --act stopped --subject node:D over a leased D prints holder=<name>; the lease stands and reads held-not-worked once its heartbeat leaves --window; no attempt is reconciled; an event --kind cancel on D is admitted or refused exactly as it was before the report
182. `a-report-of-a-stop-ends-the-engagement-a-launch-report-began` — over an unleased :todo D with N open: report --act launched --subject node:D, and D's row prints needs-broken=true; then report --act stopped --subject node:D, and with no other command D's row prints needs-broken=false unmet=1 and check counts one fewer; a second launch report raises it again and a correct --node D lowers it; a stop report on a D that is leased leaves needs-broken=true, because the lease engages it by itself
183. `reports-are-read-in-one-ask` — three reports, one with --instead-of - and one the launch of the replay above: query --ask reports --branch open --since <rev> prints reports=3 no-verb=1 launched-unmet=1 and three kind=report rows, newest last; --max 1 prints one row and a MORE line; --node D prints D's alone; --since before the retention boundary refused as handoffs --since is; --branch closed is exit 2; a report whose --acted-at is a year old is in the answer for the revision it was written at and no other
SECTION
<<<TABLE
1	cow-root-partition	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:259	one id is in C or in O and never in both; open+closed equals the counted total; an event hand-written to put one id in both is a rule 18 finding at load and refused at the candidate gate
2	settle-keeps-id-and-evidence	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:183	a state --to done settles its task: the :id, :children, :acceptance, every evidence event and its five fields read the same before and after, from the closed index, and verify prints the same verdicts over it
3	settle-moves-no-required-set	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:245	a task finishing leaves its parent's required set, rows=, baseline-rows= and percentage exactly where they were, rule 11 green on the walk; a :cancel moves the set by the delta table's row and that row alone
4	settle-releases-the-lease	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-02-close-and-counters.lisp:519	an item settled while a lease is live reads holder=unowned at once, its :release in the lease log with the settling author and the holder it ended, handoffs printing the whole transition log, no item of C in any who answer
5	containers-settle-with-their-members	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-02-close-and-counters.lisp:278	a feature of three tasks: settling the last task settles the feature and its parent work set in the same envelope and no other container, one OK line and one request id, a crash between the two replaying all-or-none; reopening returns all three to O together; an empty container settles never
6	reopen-revives	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-02-close-and-counters.lisp:503	a :reopen of a done item returns it to O at :todo with id, evidence and generation intact, open= up and closed= down by one; a :reopen of a deferred item writes no :revive
7	revive-appends-and-counts-latest	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8660.lisp:24	the revived item's closed record and every event under it still in C after the :revive, the :revive's row carrying revived=<rev> and its newest row settles=1; settled a second time, a third row appended, settles=2 on the newest, the cursor naming that newest event revision
8	index-replayed-after-crash	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-publication.lisp:44	a session killed between a :settle and the next clip: the restart's one journal replay puts the item in C's index and out of O's tree before the first ask, open and closed sum to the same total, and a :revive after the settle recovers the item in O and its record in C
9	remove-settles-only-open-items	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:69	a feature of two leaves, one done in C and one open: node remove settles the open leaf and the feature with :reason removed, leaves the closed leaf's done disposition, evidence and settle stamp untouched, moves closed= by two not three, no rule 18 finding; a fresh-id repeat while still closed appends one typed no-effect receipt changed=0; a removal over a subtree holding a live lease is refused, naming the holder
10	working-is-a-view	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-02-close-and-counters.lisp:553	|W| <= |O| over a set where every item is leased then released; a :doing item with no live lease is disposition=pending; no verb writes W
11	settle-outside-the-digest	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-01-reader.lisp:168	one state --to done digested by two independent serializers to one value, two :stamp:s and two settle event ids, the retry answered by the original OK line
12	closed-paged-without-full-load	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:499	a closed listing over a thousand settled items with --max 20 reads twenty rows, prints MORE naming --after, the next page reads the next twenty; parses=0 replays=0 and the whole history never loaded
13	closed-row-with-archive-absent	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:485	the worked acceptance's ask answering the same four rows with the retention archive file absent, and an ask that reaches for an archived body printing gap=<n> and one QUERY NOTE coverage-gap rather than a shorter list
14	merged-is-not-distributed	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:6	a fix in C with landed=<sha> and released=- while its release task is open, and released=<version> on the same row once that task settles
15	branch-and-window-required	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1517	a query --ask with no --branch refused at exit 2 naming the flag; --branch closed with no --from/--to refused the same way; --from under --branch open refused; who, stale and handoffs refused under --branch closed and --branch root
16	findings-across-c-and-o	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:314	the worked acceptance asserted line by line: four ids, four rows, open=1 closed=3 closed-in=3, no id twice, dispositions distinct, the release task listed pending by the second ask
17	clip-names-the-index-that-overflowed	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-8603.lisp:81	a clip refused with snapshot=, retained=, index= and closed-index= all printed and lower --retain or raise --max-bytes named as the one remedy, the lower --retain then passing; an index page that would pass --page-bytes or --page-records split into a further page by the clip that writes it
18	default-window-opens-two-days	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:416	a default closed-history listing at early morning, midday and exactly 00:00:00Z opening at most two UTC day partitions and, at midnight, one; no partition older than the window opened for it; a --from reaching back a month opening exactly the days that hold closure records and printing pages= accordingly
19	busy-day-many-segments	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1496	one day holding many bounded segments read in bounded pages, --max capping the rows, MORE naming --after, the day never read whole
20	history-grows-startup-does-not	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:446	O, the recent-window volume and the page bounds held fixed while the old history grows: startup resident bytes, segment bytes read, parses, replays and emitted bytes do not move; index pages read bounded by the index depth, never by volume within one depth, no whole-C load, no directory scan, no whole-history dedup
21	one-revision-publishes-together	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-publication.lisp:15	a clip staging its segments, verifying the hashes its manifests name, then committing the snapshot, both index roots and every file they reference in one commit; a kill before the commit leaves the previous root whole; a kill after the commit and before the push leaves work locally durable and unshared, reconciled against the exact remote commit
22	absent-day-is-not-a-gap	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-publication.lisp:142	a day with no manifest inside a complete manifested range answering its rows with gap=0 and no note
23	missing-segment-is-a-gap	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-publication.lisp:160	a segment the committed root names, removed: the listing answering what it can with gap=<n> and one QUERY NOTE coverage-gap, never an empty closed set
24	as-of-refuses-unavailable-partition	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-8603.lisp:17	a state-as-of ask whose day partition cannot be opened refused at exit 1 naming that one partition, never answered from a later row
25	as-of-reconstructs-settle-revive-settle	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:279	one id settled on day A, revived on day B and settled again on day C: asks ending inside each interval answering that interval's state, the three answers different, the earlier two unchanged, each read one bounded lookup over that id's chain
26	cursor-pinned-across-a-new-settle	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:331	a paged closed listing with another item settling between two pages: no row missing, no row twice, the cursor's pinned revision honoured, a continuation whose pinned revision can no longer be served refused page expired
27	activity-and-state-are-two-counts	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:300	an item settled and reopened inside one window printing settles-in=1 revives-in=1 items-in=1 closed-in=0 with open= counting it once, the same window's closed= unchanged by the reopen
28	dedup-page-unavailable-refuses	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:102	a retry whose dedup page cannot be read refused dedup unavailable, applying nothing, admitted once the page is readable again; a retry across a day rollover, a clip and a successor handoff refused already applied; a reused id with a different payload still refused reused with a different payload
29	rule-2-unavailable-is-not-green	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8649.lisp:120	a reference into C whose partition cannot be read reported rule 2: unavailable partition=<yyyy-mm-dd> at exit 1, distinct from dangling, and validation never green over history it could not read
30	wire-integers-are-strings	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:558	an id, a revision, a counter and a token total each above 2^53 crossing the wire and returning unchanged; a frame carrying a JSON number refused; null and an absent key reading alike, an empty string and an empty array reading as values
31	protocol-version-negotiated-or-refused	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:700	a client offering an unsupported version refused with the supported list named and the connection closed, no request admitted before the handshake, an oversized frame refused with one framed error before the close
32	pipeline-replies-are-correlated	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:795	pipeline two different queries, a mutation and a long-operation acceptance, deliver their response frames out of order and in fragments: every response reaches only its matching request, the operation id remains distinct; unknown, duplicate and absent response ids close the connection without falsely settling any outstanding request; malformed input with no decodable id receives a null-id refusal; reconnect after a lost mutation response applies no second event
33	disconnect-is-not-a-rollback	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:959	a client killed after its mutation was journaled: the event stands, the same request id and body returns the recorded disposition, the same id with different arguments refused; rev= and pushed= distinct in every response
34	no-effect-mutation-is-journaled	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1655	a mutation whose patches are all no-ops: the event id recorded, journal length +1, changed=0 on its OK line, the domain-projection digest unchanged while the event revision advances by one; a fresh-id invocation while still closed is the no-effect case: one typed receipt, changed=0, no second :settle, no scope, membership or counter change
35	operation-survives-the-client	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:995	a long import returning an operation id, the CLI exiting, the work continuing, the result retrievable by id afterwards, operation wait timing out while leaving the operation running
36	status-answers-while-io-runs	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:210	status and cancel answered within their bound while a busy capture, export and clip are in flight, queues, staged bytes and retained results bounded, a restart reconciling the operation ids that were pending
37	cancel-is-a-request-not-an-erasure	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:407	a cancellation acknowledged with its own final disposition, erasing no accepted mutation, reporting an uncertain external effect as uncertain rather than as cancelled
38	undo-appends-and-preserves	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:362	an undo of a named request appending a typed compensating envelope with its lineage while the original event and every receipt stay exactly where they are
39	redo-refuses-a-stale-plan	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:6	a redo whose preconditions moved refused atomically, naming what changed, writing nothing, never reached by deleting the undo
40	dry-run-writes-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8643.lisp:16	a dry run that validates and projects without mutating: after a green preview at revision R, the --request id is still new to the dedup index, SESSION OK counters unchanged; an accepted mutation moves the revision to R+1; a real apply --expect R refused stale, an apply at R+1 newly validated
41	undo-refuses-an-external-effect	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1676	an undo over a sent message, a paid execution, a publication and a source deletion refused and reported as an external effect with its own compensating workflow; shared Git history never reset as the undo path
42	every-field-has-an-owning-verb	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8661.lisp:88	every canonical field mapped to its owning typed mutation or marked derived or immutable, with no generic set-field escape hatch and no parallel alias
43	open-count-is-read-not-computed	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-01-reader.lisp:220	mutate, then ask |O| repeatedly: zero visits, zero parses, zero replays, the counter equal to an independent full count after a close, a reopen and an import replay; the open-issue and open-leaf counters separate and neither labelled |O|
44	no-friend-name-in-the-tool	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1452	the binary, its defaults and its shipped fixtures carrying no friend, bench, repository or house name; every identity arriving as configuration (the spec itself says this is not a test of this document)
45	roles-are-configured-not-inferred	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:464	a role read from CONFIG and never from the underlying model; a model capability never cancelling an agreed limit
46	reserved-role-is-not-spent-on-routine-work	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:377	an essential-security-only reserved role, a specialised different-perspective reserved-plan role and an agreed participation each expressible, and the reserved ones not spent on routine work
47	dispatch-ack-and-ownership-are-three	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:778	dispatch, delivery, acknowledgement and accepted ownership distinguished; a pending offer reserving only declared capacity; a timeout alone launching no duplicate while the old worker may run; a return reconciling before new dispatch
48	requested-model-is-not-observed-model	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:354	unknown staying unknown, concurrent attempts keeping separate model and usage attribution, a friend's usual model never standing as proof of a delegated task's executor
49	a-retry-does-not-overwrite-its-attempt	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:971	a retry never overwriting its attempt's model and usage attribution
50	silence-is-a-ping-not-a-verdict	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:26	the configured silence threshold triggering one bounded ping, a nonresponsive capacity marked unavailable with reason unconfirmed and no claim of sleep or exhausted credit, a failed probe read as unresolved delivery
51	explicit-rest-is-not-pinged	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:870	explicit rest respected, not pinged
52	return-reconciles-before-dispatch	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:418	a return reconciling before new dispatch
53	four-facts-four-verbs	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-fleet-assignment.lisp:165	an admitted offer with declared free slots writing :effect :dispatched, a pending-offer index entry and a reservation, and leaving node state, W, the lease index, attempts, evidence and completion unchanged
54	offer-writes-intent-and-a-reservation	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:2031	acknowledge --stage received writing delivery only; nothing inferred from anything
55	a-receipt-needs-a-verifier	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-fleet-assignment.lisp:237	a copied note and an --as <recipient> with no verifier result refused with no canonical write
56	staged-admission-refuses	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:61	a verifier or payload reader that returns after a conflicting revision, or fails validation, writing no reservation, receipt, lease or W change while session status answers inside its bound
57	accepted-creates-one-lease-or-binds	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:2107	an accepted receipt after received creating exactly one :lease and one W entry, or binding a second attempt to the same holder's lease with its deadline unchanged, converting and never doubling capacity, an absent observed model recorded unknown
58	no-shadow-lease-across-holders	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:2066	a cross-holder offer or accepted reply refused or retained late and creating no lease
59	until-is-overdue-not-released	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:2160	at --until and at lease expiry no duplicate launch and no stopped or completed claim, the reservation and any uncertain execution retained until reconciled
60	late-and-duplicate-receipts-are-retained	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:2200	a late accept after a decline, a replacement, an expiry or a generation change retained :late, reviving no lease and overwriting no successor; the same request replaying its success, the same verified receipt under a new request consuming no capacity, conflicting bytes for one receipt id refused
61	stop-is-a-hold-not-a-cancel	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-holds.lisp:9	execution stop --node writing a hold and directives and no transition, goal show still printing stop=none
62	one-stop-note-cannot-cancel-two-attempts	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-holds.lisp:35	state --to cancel-requested then an event --kind cancel whose evidence covers one of two live attempts refused, the same with both covered admitted
63	hold-survives-a-crash	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-holds.lisp:59	a crash after the hold is durable and before capture or send recovering the same hold and target identities with no duplicate launch
64	capture-survives-clip	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-holds.lisp:86	a clip completing between the anchor and the manifest, the pin resolving the same revision and span, an unrepresentable pin refusing admission before EXECUTION OK
65	no-dispatch-slips-past-a-hold	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-holds.lisp:125	an offer prepared before a pause refused at the last send; a launch, a correction and a move raced against a scope pause all held
66	held-acceptance-converts-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8640.lisp:17	an acceptance under a hold retained :accepted-held with no lease, launch or release until reconciled, and a lift of the hold converting nothing
67	reconcile-preserves-contradiction	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8640.lisp:57	live, expired, pending, paused, unsupported and unreachable targets captured exactly through the indexes in bounded pages; duplicate, delayed and out-of-order receipts and a forged source inferring no ownership, release, cancellation or completion; two contradictory observations kept unresolved; a stop report carrying no synthesised zero usage; two attempts on one task stopped by two receipts, usage retained apart, the task counted once in W; the holder-only release rule unbypassed by a confirmed exit
68	resume-is-two-actions	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8640.lisp:131	release-hold lifting only its control's hold with an overlapping hold still effective and no process resumed; resume-workers refusing an unsupported capability before sending and holding until a running observation, a delivery receipt alone releasing nothing
69	correct-is-a-linked-segment	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8640.lisp:162	one envelope writing the hold, the :correct and the instruction binding, a retry bumping the generation once; a worker's old-generation usage kept as its own segment
70	bare-correct-refuses-under-execution	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8640.lisp:196	the bare correct refused by name while an attempt is live and admitted once none is
71	endpoint-is-local-and-private	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:197	the session's directory created 0700 and its socket 0600, both owned by the account that runs it, a pre-existing directory or socket with wider modes refused rather than reused, the Windows named pipe created with FILE_FLAG_FIRST_PIPE_INSTANCE, and no listener bound to any network address
72	four-capability-groups-and-three-fields	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:910	child agents, swarms, local models and one-shots each expressible as a capability group with a stable id, its source, its last-verified stamp, its availability and its constraints; declared support, verified runtime and current free capacity kept as three fields that never collapse into one
73	unchanged-config-is-one-bounded-answer	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:274	the config exchange bounded, validated and atomic, with no roster and no prose repeated per poll and no secret in a manifest
74	an-invalid-delta-leaves-the-old-config	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:302	an invalid config delta leaving the old config intact
75	a-partial-manifest-is-refused	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:330	a partial manifest refused
76	pricing-is-pinned-by-revision	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8647.lisp:115	an old estimate reproducible after a rate change
77	unknown-price-is-not-zero	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8651.lisp:15	a missing dimension unknown, never zero
78	subscription-is-not-free-reference-cost	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8650.lisp:152	a subscription not read as free reference cost; the three cost values kept separately labelled
79	local-tokens-cost-zero-api	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8646.lisp:38	local tokens cost zero API
80	roadmap-outlives-its-work	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-03-containers.lisp:636	an epic completed, clipped, restarted and advanced past 24 hours, then its roadmap listed and opened, rendering identical historical rows and retrieving the exact code and test receipts without loading all of C
81	roadmap-opened-after-the-window	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-03-containers.lisp:657	completed rows still in the table and remaining only an explicit filter; a referenced task reopened and a new sub-feature added, the affected rollups updated without dropping a completed member and without double-counting a shared prerequisite, the summary and expanded-row views exercised over the same ids
82	historic-tick-survives-a-source-change	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8645.lisp:248	a changed source or criterion preserving the historic tick at its pinned revision while the current view requires re-verification
83	regression-opens-repair-work	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8648.lisp:24	a confirmed regression creating linked open repair work
84	unrelated-receipts-stay-reusable	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8651.lisp:136	unrelated receipts untouched
85	chat-and-file-render-are-byte-identical	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-roadmap.lisp:421	one projection and revision rendering the same bytes to chat and to a marker region, every other byte preserved
86	render-refuses-a-target-outside-its-roots	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:214	a missing, duplicate or reversed marker pair refused, and a target outside the configured permitted roots refused rather than guessed
87	restore-is-isolated-and-dispatches-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-17-savepoint-restore.lisp:181	a restore taking no ownership, reanimating no assignment and replaying no message
88	a-savepoint-is-not-a-shared-backup	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8641.lisp:59	savepoint age, the local and the shared revisions, unshared work and failed backups all readable, a savepoint never printed where a checkpoint was asked for
89	compaction-keeps-the-last-copy	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8643.lisp:81	compaction never removing the only recoverable copy
90	new-verbs-have-a-kind-and-a-field-order	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1357	undo, redo, friend, model, observe and config --intake each writing an event of its own kind, with every field of its list written in order and absent ones written (:absent), :node written (:absent) on all six, and a second build serializing each one to the same bytes
91	new-verbs-retry-to-one-event	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1399	each of the six replayed twice under one request id yielding one event: the second call answered with the original OK line, applying nothing, across a clip and across a successor handoff; the same id with a changed payload refused reused with a different payload; each OK line carrying the subject its kind names and never an empty node=
92	absent-empty-and-null-are-three-spellings	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-03-containers.lisp:581	one request giving no :members, one giving an empty list and one giving a wire null digesting to three values of which the first and the third are equal and the second differs; the same three surviving a wire round trip unchanged
93	clip-is-one-long-operation	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:455	clip returning OPERATION OK id= op=clip and exiting, the transport continuing, operation wait --id printing the CLIP OK line with that operation=<id> and its pushed=, a raced transport printing CLIP RACED through the same wait, and session stop printing the same CLIP OK first and then SESSION OK by waiting on its own operation inside --git-timeout
94	undo-names-its-reversible-set	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1779	every row of the reversible-verb table exercised: each reversible verb undone by the envelope the table names, each refused verb refused not reversible here naming itself, an undo over event --kind cancel and over node remove refused because both dispositions are terminal, and no undo reaching a terminal state by any path
95	add-field-order-is-complete	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8641.lisp:186	a node add with every one of the twelve fields given, one with each absent, one with --links-empty and one with --clear-links, digested by two serializers to one value each and to four distinct values; a fixture in the pre-fold order refused schema revision unsupported at load, never read as the new shape
96	repo-only-at-the-root	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1958	--repo with --under-root open --type work-set accepted and unique; the same --repo again refused repo held by <id>; --repo under a parent refused repo outside root; node edit and node move unable to change it
97	roadmap-has-one-creator	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:505	node add --type roadmap exit 2 naming roadmap create; roadmap create writing one node and one view in one envelope, a crash between them replaying all-or-none; no second alias
98	metadata-patches-preserve-intent	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1555	keep, clear, set-empty, set-false and set-value on each of the five fields round-tripping and digesting distinctly; an all-keep, a malformed tag and a wrong type refused with no event and no counter moved; --version set on a :feature refused version on a feature and its keep accepted
99	edit-is-atomic-and-replayable	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1619	a bad one-of-five patch writing nothing; an accepted mixed edit moving only its named fields and the category index; the same request id retried answered by its original NODE OK; a changed payload refused; an equal-value edit the no-effect receipt, changed=0, rev up by one
100	edit-undo-preserves-later-work	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1892	an edit undone restores :before; the same undo after an intervening edit refused conflict, both events standing
101	edit-never-fetches-a-link	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1926	a link that is a live URL to a counting endpoint added, edited and rendered with zero requests observed; a link holding NUL refused bad link; a refusal on a private node printing no value
102	move-keeps-every-count	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1989	a required subtree moved between two features: the source's and destination's required sets and open counts move by the subtree, the common ancestor's net count is stable, |O|, |C|, W and every task state unchanged, rule 11 green on the walk, and no whole-set scan (visits asserted)
103	move-same-parent-is-a-receipt	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1237	--from equal to --under and true: the structure event alone, changed=0, sibling order unchanged; a lost reply retried yields one envelope; a different payload under the id refused; every refusal leaves both parents unchanged
104	move-refuses-by-name	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1193	a wrong --from, a destination inside the subtree, a root container, a repository root, a shared container, another repository, and a roadmap as either parent, each refused with its named reason and the identity, nothing written
105	move-keeps-the-lease	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1140	a working subtree moved with its effective :responsible unchanged keeps the same lease, attempt and usage; a move that would change it over an unreconciled attempt refused active context change naming the ids; a move under a public parent from a private one refused privacy reduction, the reverse admitted and the public render losing the rows
106	move-updates-every-roadmap-scope	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-roadmap.lisp:467	a row referenced by two roadmaps outside both parent chains and one unrelated roadmap: both referencing scope revisions advance in the envelope, the unrelated one stays, a failed acceptance moves none, historical renders keep the old captured scope, an intervening affected-roadmap mutation makes undo conflict
107	move-undo-refuses-a-reorder	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:1269	undo after a sibling reorder, a further reparent or a privacy change refused conflict and guessing no position; undo otherwise restoring the exact before order and required sets with fresh scope revisions, the old numbers unwritten; a kill around acceptance and during clip exposing neither two parents nor none
108	axisless-history	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-roadmap.lisp:13	two ordered rows added by roadmap row, one finished, the state exported and loaded, the view reopened past the default window: both rows and their evidence present, the denominator not reduced by completion; a row retired records a scope movement, keeps its node, and the prior view reconstructs at its captured revision
109	matrix-retirement	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-roadmap.lisp:96	axis --remove of a first-axis row and then of another axis's member: only the selected coordinates retired and recoverable, no task cancelled, an unknown member refused, a layout change on a populated roadmap refused layout populated with no partial write, and a matrix never flattened without explicit selections
110	configure-no-effect-and-undo-conflict	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-roadmap.lisp:201	an equal-value configure, its reply lost, a later edit, then the retry: the original receipt returned and the later value kept; undo restoring an ordered preimage only while its guards match
111	completed-view-mutation	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-roadmap.lisp:327	metadata, projection and render on a settled roadmap reviving nothing; an outstanding member added applying the atomic revival rule so no settled container silently holds open required work; counts and indexes checked by the reference fold after each step
112	render-artifact-is-bounded	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:148	ordinary replies and a --chat artifact interleaved in one correlated batch, request ids, byte length and hash verified; a corrupt or oversized artifact a bounded refusal and never partial Markdown; --check creating no target, no receipt claiming a write, no commit and no push
113	a-root-id-grants-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-07-replays-mid.lisp:277	a stored permitted root with no --render-root mapping refusing file mode while --chat renders; an escaping path, a symlink escape and a target identity other than the mapping's refused; the cooperative lock exercised and its external-editor limit retained
114	priority-orders-only-the-eligible	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-06-replays-early.lisp:94	a blocked rank-0 task stays blocked with its reason and resolver while a rank-9 ready sibling is first among the eligible; --order priority under done exit 2; capacity loss, approval withdrawal, a dependency change or a hold rechecked before ranking and starting or interrupting nothing
115	priority-inherits-and-clears	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-06-replays-early.lisp:48	a root :subtree rank changing ready order with no lease, attempt, state, O, C, W, counter, baseline or roadmap moved; a child's :self overriding it; a clear revealing the parent; settle and reopen keeping the slots; a move re-reading inheritance with no cloned event
116	rank-2-precedes-10	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-06-replays-early.lisp:183	ranks compared as integers, and equal and default rows ordered by id across a restart, a handoff, a cursor continuation and skewed clocks; a first unseen filter O(k log k), later pages from the pinned order, a subtree invalidation touching no unrelated scope and no C
117	priority-undo-is-history-not-value	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-06-replays-early.lisp:137	a same-value set and a clear of an absent slot each the no-effect receipt; set 2, set 9, set 2, then undo of the first refused although the value matches
118	priority-grants-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-06-replays-early.lisp:23	with priority set on every node, who unchanged, no lease written, no worker selected, no approval bypassed
119	state-export-describes-exactly-r	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:66	capture R while R+1 is accepted and the bytes describe R; an exact-snapshot export with B equal to R and an absent end, and one with B below R over a multi-record prefix across a rotation; an absent end below R, a missing or swapped record, a wrong end hash or revision and a cut inside an envelope each refused, and a present later tail never replayed
120	state-export-refuses-a-gap	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-state-export-replays.lisp:11	a missing mandatory member, a changed digest, a dangling internal reference, a path escape, a symlink, an output overrun and a corrupt S-expression each refused with no valid load; a historical export whose resolver observations are gone refused with a named proof gap and never given current ones; --closed-history range over [from, to) declaring its omissions while keeping closure, all reaching C past the resident window and a fresh load reproducing its proof
121	state-export-is-one-long-operation	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-08-replays-late.lisp:138	an export blocked on archive I/O acknowledging its operation at once, status, cancel and an unrelated mutation responsive under it, and wait returning the same operation and captured revision after publication or refusal; an export inside an atomic batch refused by entry id
122	state-export-pin-survives-clip	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-state-export-replays.lisp:76	capture R, a clip and a retention pass at R+1 during the copy, then exactly R completed or a recovery gap named; no pinned member reclaimed, no current bytes substituted
123	state-export-disconnect-and-cancel	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-state-export-replays.lisp:115	a lost client, a restart and a cancellation around the no-replace publication keeping one operation and one output identity, no duplicate directory, no claim to reverse a published one; an existing destination refused; a staged manifest before the commit not published
124	state-load-is-isolated	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-state-export-replays.lisp:159	an instrumented export and load with no ownership change, no dispatch, no replay, no merge, no resolver run, no network and no repository write, only the declared exclusive paths written; the loaded snapshot answering query --snapshot and refused as a --session by every mutation, replay, clip and handoff verb; a re-export comparing equal in every field, id, Unicode, empty and absent value, role, CONFIG, ACTIVE, O, C, roadmap and accounting record
125	fenced-export-can-finish	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-state-export-replays.lisp:252	in a fenced session an export started, its status and terminal line read by its id, and separately an unfinished one cancelled with publication reconciled; an unknown id, a non-export id, operation list and every canonical write refused fenced; no second owner and no mutation authority created
126	goal-crosses-harness	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8644.lisp:158	G a :doing leaf at revision r; harness A as coordinator C: goal set --goal G --expect r prints GOAL OK rev=r+1, writes a (:coordinator "C") note with a :deny on a made-up model for :coding, goal update --stop on G; harness B another build shows --as C against live and clipped snapshot: both print goal=G, a rev= at or after every write of A's, stop=requested not cancelled, the same note id and constraint row byte for byte; B's update --progress refused stop requested, nothing written; A writes event --kind cancel --evidence; B's next show prints stop=cancelled
127	goal-stale-update-refuses	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8645.lisp:40	A and B both show at revision r; A writes update --progress with the evidence triple on G, a :doing leaf, r+1; B's update --progress --expect r refused GOAL FAIL expect=r current=r+1: stale, snapshot unchanged, B's next show prints A's evidence row; A then update --stop --expect r+1 (r+2); B's update --progress --expect r+1 refused stale, show prints stop=requested; B's update --progress --expect r+2 refused stop requested; stop=requested stands until a :cancel with evidence or state --to doing by id; A goal set to a node on the closed branch refused disposition=done whatever --expect says
128	goal-stop-is-a-request-not-evidence	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8645.lisp:109	G :doing at r; goal update --stop --reason --expect r prints GOAL OK change=stop kind=transition rev=r+1, the event a :transition :to :cancel-requested carrying :reason and no :evidence; check has no finding; state --to cancel-requested on a sibling writes an event of the same kind and fields; state --to doing by id admitted (withdrawal), show prints stop=none; stopped again, then event --kind cancel --evidence, show prints stop=cancelled terminal, goal set refused disposition=cancelled; goal update --stop on a :review node refused no edge, as state --to cancel-requested is there, and on a :done node the same
129	goal-update-writes-only-existing-kinds	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8645.lisp:179	every goal update form written, then the journal read: each event a :transition or an :evidence with exactly the field list of its kind, on the goal node of the scope and no other node; --progress <text> alone on a :todo node writes :to :doing with the text as :reason (the same event state --to doing --reason writes) and on a :doing node refused no edge, nothing written; --progress with the evidence triple on a :doing node writes the :evidence event evidence would; goal set and goal set --clear each write one :goal event with :node (:absent); accept --add moves its scope revision and goal update never does; a retried set with the same --request id and payload returns the same event id once
130	goal-expect-is-required	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8644.lisp:225	goal set and goal update without --expect exit 2 naming the flag, nothing written; with --expect at the current local revision admitted; with --expect one behind refused stale at exit 1 with the current value printed; --dry-run with the stale expectation prints the same refusal and writes no event, no journal revision and no dedup entry; a show never takes --expect and answers at the revision it prints
131	applicable-cap-never-hides-a-deny	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-05-durable-journal.lisp:115	N active notes, N > --max, the only :deny in the note that sorts last (Delegation's applicable); applicable --max 1 --candidate <role>/<model> prints excluded <that id> and NOTES MORE; goal show --max 1 prints the same constraint row before any cut row, then GOAL MORE; with the notes index unloadable, both print FAIL and neither prints eligible nor any row
132	savepoint-cut-never-splits-an-envelope	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8649.lisp:153	a two-event request accepted and a savepoint after it: both events represented once in the image and a retry answered the byte-identical original lines; a savepoint whose cut would fall between the two events refused cut inside an envelope and no image published
133	crash-after-append-recovers-the-reply-once	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-03-containers.lisp:606	a crash after the durable append and before the reply: the envelope replayed once from the record, the same request and payload answered with the original OK, the retry mutating nothing; the same id with a changed payload refused reused with a different payload after the restore as before it
134	rotation-keeps-one-journal	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8649.lisp:54	a clip rotating the journal after a savepoint: the new segment's header naming the same journal id and the copied boundary record with its sequence and hashes, the chain continuing, the savepoint's cut and every disposition it needs reachable without a scan of old segments, session export --journal writing the same bundle from either file
135	torn-tail-is-diagnosed-not-truncated	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:6	a partial frame at the end of the journal, a flipped bit inside a frame and a wrong header each refused with the file bit for bit as it was, the first reported recovery-gap kind=torn-tail, the second kind=corrupt-record and the third journal mismatch, none rounded to another, every mutation refused journal uncertain after a failed append until the tail is read
136	replay-mints-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:72	a replay of five envelopes reconstructing the exact root digest, the closed rows and the next revision with no fresh event id, no clock read and no verb run; a replay failing midway leaving its target untouched
137	savepoint-write-failure-keeps-the-previous	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8649.lisp:238	the image write, the manifest publication and the sync each failed in turn: the previous verified savepoint restores, and savepoint list prints the attempt verdict=failed
138	copied-journal-grants-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8643.lisp:130	a journal and its savepoint copied to another bench: savepoint restore there inspects in isolation, takes no ownership and dispatches nothing, and a session start over the copy is refused by the fencing rules, the journal id notwithstanding
139	reply-retired-only-under-verified-coverage	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8648.lisp:67	a request whose events lie before the clip boundary: its retry answered already applied only once the committed snapshot, its retained events and the dedup root the boundary record names are verified reachable; with the push failed or the root unreadable, the original OK still answered from the retained disposition or recovery-gap kind=coverage-unverified printed, and never a reply invented or deleted
140	indivisible-record-refused-before-ack	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-8603.lisp:43	one key with its locator that no page under --page-bytes could hold refused indivisible at exit 2 with nothing journaled, while a request whose repeatable evidence would pass the bound is admitted and chunked behind a detail root; session start --page-records 2 refused at exit 2 naming the flag
141	days-merge-by-revision-never-concatenate	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:352	a closure backdated by --now into an earlier day: its row in that day, and a --from/--to over both days printing rows in revision order across the day boundary; the same day clipped in one batch and in ten yielding identical leaves
142	page-budget-is-not-max	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-04-doing-and-journal.lisp:379	a filtered historical ask whose filter rejects every row read: QUERY MORE shown=0 pages=<n> at the budget, the whole history never scanned, --max untouched by it, the continuation answering from the same captured root
143	overlay-is-bounded-and-rebuilt	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-09-replays-publication.lisp:123	a recovery replaying a thousand settles into overlay pages under --index-cache, an eviction under load losing no row, the next clip writing them into the pages, and no query replaying the journal
144	duty-tier-executes-the-policy	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8660.lisp:98	a resident session (E02) given an approved finite policy record from C, :by on every rule, executes it with the cheapest qualified model or none, authors no policy of its own, and escalates every judgment it cannot make; a policy rule with no :by is refused at load
145	escalation-carries-rule-default-age	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8660.lisp:140	an escalation row carries the policy rule that could not decide it, the default that fires on silence, and its age; stale reads the three and reassigns nothing
146	wait-table-four-presence-columns	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8660.lisp:160	the per-harness wait table carries the four presence facts -- process alive, beat written, delivery handled, parent woke -- and a harness with the fourth unproven holds no resident session, only a duty session driven by notes
147	quiet-time-calls-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8660.lisp:182	a resident or duty session with nothing changed makes no model call and sends no note; state is published mechanically; the cost of one event is the measured spend of the one call that event caused
148	cost-per-accepted-decision	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8661.lisp:15	the coordination measure is cost per accepted decision across tiers, with wrong or missed decisions and recovery latency as gates; the decisions-per-token sentence of Decision packets is replaced
149	single-writer-kernel-total-order	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8661.lisp:52	two concurrent clients' commands to the one kernel land in one total order; the journal shows the sequence numbers in that order; a mutation outside the command loop is a defect (the validator rule)
150	machine-is-config-and-never-a-work-tree-node	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8661.lisp:179	nova-work machine --register m-a1 --name studio --owner glenn --connect profile:studio --role build --role test --permit go-test --limit cores=16 --fact os=macos --declared-by glenn --reason "studio online" prints MACHINE OK id= request= machine=m-a1 rev= pushed= changed= emitted=; |O|, rows=, every roadmap and every count unchanged, the event's :node (:absent), the record a :kind :machine member of the fleet section of CONFIG, and no verb can settle it, :to :done it or make it completion evidence
151	allocation-binds-machine-slot-generation	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8662.lisp:65	nova-work take --session <path> --machine m-a1 --node schema/cpp/refuse-newer --slots 1 --offer <offer-id> --attempt <attempt-id> --generation <n> --request-ref <opaque-id> --batch <request-id> prints ALLOC OK id= request= machine=m-a1 allocation=<id> slot=1 node=... batch= offer= attempt= machine-generation=<n> allocation-generation=<g> rev= pushed= changed= emitted=; the allocation is ACTIVE data referencing the machine's CONFIG identity and revision, the (offer, attempt) is the offer section's reservation key, admission happens before preparation and both capacity constraints validate atomically in one binding, and the core-pin rule stays a declared :limits constraint and is not in the allocation
152	allocation-take-is-atomic-and-idempotent	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8662.lisp:134	take with --slots 2 prints ALLOC OK allocation=<id> slots=2; the retry under the same request id prints the original ALLOC OK line and applies nothing, a changed payload refused reused with a different payload, a take for more than the declared :concurrent refused whole ALLOC FAIL machine=m-a1 slots=2 holder=<name>: capacity at exit 1 with no partial grant; heartbeat prints ALLOC HEARTBEAT OK allocation= machine= machine-generation= allocation-generation=; a heartbeat with a stale allocation or machine generation prints ALLOC FAIL allocation=<id>: stale token; release prints ALLOC RELEASE OK allocation= machine= slot= freed=true and frees exactly that slot; list --machine m-a1 prints one ALLOC ROW per live allocation
153	expiry-marks-suspect-reuse-needs-fencing	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8662.lisp:269	an allocation past its deadline reads suspect: a renewal and a take with the stale token refused ALLOC FAIL machine=m-a1: suspect since=<stamp> at exit 1, the uncertain ACTIVE capacity retained and never cleared by the expiry; only a verified stop observation, a bound not-started or machine-side fencing releases the slot, and until one of those the refusal is ALLOC FAIL machine=m-a1: not fenced at exit 1
154	probe-records-observed-active-and-touches-no-config	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8662.lisp:319	nova-work probe --session <path> --machine m-a1 --slot 1 --source <pointer> prints PROBE OK machine=m-a1 slot=1 fact=observed at=<stamp> source=<pointer>, and an unestablished fact prints fact=absent; query --ask fleet prints the same declared :facts with their declared-by and declared-at before and after, no :limits, no :facts, no :connect and no :roles change, the profile never resolved, and no credential in any record or clip
155	one-allocator-per-machine-aliases-share-nested-conserve	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8662.lisp:394	a second allocator for one machine refused ALLOC FAIL machine=m-a1: allocator held at exit 1; an allocation taken through one alias of a host reads as the same allocation under the other alias and counts once; an allocation nested under another allocation's scope draws from the same declared capacity, never an additional slot, ALLOC ROW listing each live allocation once
156	release-one-allocation-spares-the-other	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8663.lisp:138	machine m-a1 with :concurrent 2 holds alloc-a (slot 1, node N1) and alloc-b (slot 2, node N2); release --allocation alloc-a --generation <n> prints ALLOC RELEASE OK allocation=alloc-a machine=m-a1 slot=1 freed=true and list --machine m-a1 then shows exactly one ALLOC ROW for alloc-b with slot=2; the released slot 1 is free for a new take --slots 1 while alloc-b continues
157	stale-allocation-id-refused-by-name	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8663.lisp:188	after alloc-a is released, a heartbeat or release using --allocation alloc-a --generation <n> refused ALLOC FAIL machine=m-a1 allocation=alloc-a: stale token at exit 1, naming the allocation id that no longer matches any live allocation; a take using the released allocation's generation against a changed machine configuration refused ALLOC FAIL machine=m-a1 slots=1 holder=<n>: capacity at exit 1 by the stale machine generation check
158	preparation-interrupted-before-launch	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8664.lisp:21	take allocates slot 1 on m-a1 to node N1, allocation alloc-c; preparation (clone/scp) interrupted before the model launches; a second session's take --machine m-a1 --slots 1 for node N2 refused ALLOC FAIL machine=m-a1 slots=1 holder=<N1>: capacity because alloc-c still holds the slot; only after reconciliation (a verified stop observation, a not-started rejection, or machine-side fencing) frees alloc-c does the slot become available, and no second session reuses the capacity until then
159	concurrent-slots-refuse-third-job	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8664.lisp:71	machine m-a1 with :cores 16 and :concurrent 2 holds two allocations each consuming one slot; a third take --machine m-a1 --slots 1 --node N3 --generation <n> refused ALLOC FAIL machine=m-a1 slots=1 holder=<name>: capacity at exit 1 even though fourteen cores sit idle, because slots are bounded by declared concurrency and cores are a separate constraint validated independently
160	route-config-lists-key-by-path-never-value	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-10-routes.lisp:29	nova-work route --register anthropic/claude-3.7-sonnet --provider anthropic --endpoint https://api.example.com --key-location (:env "ANTHROPIC_KEY") --plan metered --cost-per-mtok 15 --capabilities (:text yes :code yes :tool-calls yes) --owner glenn --reason "team route" prints ROUTE OK id= request= route= rev= pushed= changed= emitted=; query --ask routes lists the member with every field but the key -- the key is a path or an env name and no value is ever printed, stored or echoed; a :key-location that is neither refused credential in record with the value never echoed; |O|, rows= and every count unchanged, the event's :node (:absent), and the record a :kind :route member of the routes section of CONFIG
161	routing-picks-flat-before-metered	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-10-routes.lisp:125	a card class whose eligible routes are one metered, one free and one flat projects flat first, then free, then metered; two metered routes order by :cost-per-mtok ascending, and a tie by bytewise stable id; a route listed twice in the class's list is two shares of its routing; the projection is derived at the named scope revision and never a stored list
162	unprobed-route-carries-no-card	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-10-routes.lisp:156	a route with no passing probe -- no probe record, or a probe whose :pass is false, or a live :benched-until -- is absent from the generated cards of its class, never present with a warning, and the card builder refuses to cut a card for it; once a probe passes and the bench clears, the route carries cards again
163	three-abstains-bench-until-probe	PRESENT	lisp/nova-work	lisp/nova-work/tests/acceptance/slice-10-routes.lisp:196	a route whose probe thrice in a row answers pass=absent reads benched, its :benched-until set and its cards carried by none, and a further probe before that date changes nothing; the next probe that passes clears the bench and the route carries cards again
164	prompt-profile-expired-shows-on-the-status-line	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-8661.lisp:126	a manager whose profile expired yesterday: session status prints SESSION OK profile=sonnet profile-state=stale profile-expired=2026-09-15 profile-owner=<name>; a manager with no profile prints profile=sonnet profile-state=absent; a current one prints profile=sonnet profile-state=current profile-expires=<stamp>; a manager session names its profile on that same line; only the coordinator's profile verb writes a profile, every edit a versioned record carrying :by and its own revision
165	a-done-need-on-unverified-evidence-admits-nothing	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-785-gate.lisp:248	N settled done on a run: pointer the fake resolver has not yet answered: query --ask ready --branch open --node D prints D's row ready=false reason=need-unverified need=N unmet=1, and take --node D prints LEASE FAIL node=D unmet=1: unmet need N need-unverified at exit 1 with the journal, W and the lease index unchanged; after verify --node N against a resolver answering holds, and no other command, the same ask prints ready=true and the same take prints LEASE OK; the same run with the resolver answering absent leaves both refusals standing
166	stale-evidence-does-not-unmeet-a-need	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-785-gate.lisp:287	N met, then source --to <sha> on N's repository work set: N's evidence reads stale and N counts unknown in its rollup, while D's row still prints ready=true unmet=0 and take --node D is admitted
167	every-unmet-need-has-one-reason	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-785-gate.lisp:135	five fixtures, one per row of rule 2's table, each printing its one token, need= and the resolver that row names; over a cancelled need, dep --remove of the edge and then ready=true with no other command; over a superseded need, the remove and a dep --add of its :superseded-by; two unmet needs printing the first in :deps order and unmet=2; no fixture moving D's state=
168	a-container-need-is-met-with-its-members	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-785-gate.lisp:373	a need that is a :feature of two tasks: one done and verified and one open, need-open; both done and one on unverified evidence, need-unverified; both verified, met; an empty container never met
169	every-admission-verb-refuses-an-unmet-need	ABSENT	lisp/nova-work	-	over D with N open and no lease, offer or allocation on D: take --node, take --node --for <name>, offer, state --to doing, goal update --progress with D the scope's goal, take --machine naming an offer that does not exist, and task packet; and over a D that was leased, offered and received while N was met and whose N was then reopened: release --handed, reassign, acknowledge --stage accepted, take --machine on the real offer, task packet, and undo of D's earlier release. Each exits 1 with its own FAIL line ending unmet need N <reason> -- take --machine and task packet printing that and not unknown offer or no lease, by rule 3's order -- STATE FAIL carrying no rule number, and no lease, offer, reservation, allocation, W entry, transition, compensating envelope or packet file written; the same calls under --dry-run print the same refusals; with N met, each prints its OK; with a scheduling hold over D as well, offer prints held by control <id> and an acceptance is retained :accepted-held, which execution reconcile leaves unconverted while N is unmet; a take --node of an id the session does not hold prints that verb's no-such-node refusal and no unmet need
170	a-reopened-need-under-a-doing-dependent-leaves-the-set-green	ABSENT	lisp/nova-work	-	D :doing on N met; event --kind reopen on N; then check, a clip and a fresh session start on that clip: each prints findings=0 and no WORK FAIL line names D or rule 10, WORK OK prints needs-broken=1, and an unrelated evidence on another node is admitted after each
171	a-verb-that-can-admit-declares-its-needs-gate	ABSENT	lisp/nova-work	-	the schema file's coverage test, over a fixture schema: a verb entry whose event kinds include :lease and which carries no needs-gate fails the test naming the verb; the same entry with needs-gate: refuses passes; needs-gate: exempt with no reason fails; an acknowledge entry marked refuses whose needs-gate-forms omits --stage accepted fails; and the shipped file passes with every verb of rule 3's list marked refuses and execution reconcile alone marked withholds
172	a-removed-or-corrected-need-is-unmet	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-785-gate.lisp:415	R removed by node remove while no edge names it, then dep --add R on D: admitted, DEP OK met=false, and D's row prints reason=need-closed-unaccepted need=R; N met, then correct --node N: D's row prints reason=need-unverified, and after N's reopen, new evidence, verify and done at the new generation it prints unmet=0
173	an-attested-only-need-is-met-by-its-attestation-and-by-nothing-less	PRESENT	lisp/nova-work	lisp/nova-work/tests/replays-785-gate.lisp:326	N with one :attested criterion, done on an attest whose --result pointer the resolver has not answered: need-unverified; the pointer verified: met; after a correct, unmet again; the same attest typed under another --as meets it just the same
174	ready-true-implies-needs-met-and-not-the-reverse	ABSENT	lisp/nova-work	-	D needs-met and leased by another name: its ready row prints ready=false unmet=0 with an ownership reason and take --node D prints LEASE FAIL: held; across the whole fixture no row prints ready=true beside an unmet= above zero
175	needs-met-is-read-not-released	ABSENT	lisp/nova-work	-	N's evidence verified in the cache, then state --to done on N: after that OK line and with no further command the row of D, unowned, prints unmet=0 ready=true, and the journal holds no event whose :node is D; the same for a dep --remove of D's last unmet edge
176	reverting-a-need-flags-an-engaged-dependent-and-kills-nothing	ABSENT	lisp/nova-work	-	D leased, :doing and heartbeating on N met; E, a second dependent of N, settled in C; F a dependent of E. event --kind reopen on N: D's row prints reason=need-reverted needs-broken=true, check prints WORK OK needs-broken=2 at exit 0 with no WORK FAIL <id> line, D's lease, attempt and next heartbeat stand, task packet for D is refused by rule 3, E stays in C with every count of its scope unchanged, and F's row prints needs-broken=false; N settled and verified again, D and E read needs-broken=false with no command naming either
177	an-edge-added-under-work-flags-and-every-way-past-a-need-is-recorded	ABSENT	lisp/nova-work	-	dep --add M of an open M onto leased D prints DEP OK change=add need=M met=false unmet=1 needs-broken=true and stops nothing; onto an unengaged :todo node it prints needs-broken=false; dep --remove N, take --node D, dep --add N with N still open leaves three events with their authors, the lease standing and needs-broken=true; dep --add D on D and an edge closing a two-node cycle are each refused rule 3, an id naming nothing rule 2: dangling, each exit 1 with nothing written; a need under another repository work set of the same O is admitted; over a container need with one unverified member, node require --to false on that member prints its :require scope event, moves the container's required= by one and leaves the dependent unmet=0
178	report-records-and-changes-nothing	ABSENT	lisp/nova-work	-	report --act other --subject machine:<id> --what ... --acted-at <t> --instead-of - --reason ... under --now <t + 90s> prints REPORT OK subject=machine:<id> act=other instead-of=- acted-at=<t> lag=90s unmet=- need=- holder=-; the journal gains one :report event with :node (:absent) and its six fields in order; |O|, W, every lease, every count, every roadmap and the machine's member unchanged; a retry of the request id prints the original line, a changed payload under it refused reused with a different payload, an undo of it refused not reversible here, and two serializers digest it to one value with :unmet and :need outside the digest
179	report-refuses-by-name	ABSENT	lisp/nova-work	-	each of the six flags missing, an --act outside the three, a --subject with no : or of an unknown kind, and a whitespace-only --what, --reason, --instead-of and external: text: each exit 2 on one line ending run: nova-work help; a subject naming no node, machine, friend, route or offer, an --as that is no friend, an --acted-at ahead of the clock past --skew, and a stale --expect: each REPORT FAIL at exit 1; in every case no event, no journal record and no dedup entry
180	a-hand-launch-is-refused-when-asked-and-recorded-when-told	ABSENT	lisp/nova-work	-	over D with N open: take --node D --dry-run and take --node D both refuse at exit 1 and no flag changes either; report --act launched --subject node:D prints REPORT OK unmet=1 need=N holder=unowned, writes no lease and no transition, leaves who unchanged, and D's row then prints needs-broken=true; the next take --node D still refuses; N met, D's row prints needs-broken=false and take is admitted. The same report over a needs-met node held by another name prints unmet=0 need=- holder=<name>
181	a-report-of-a-stop-releases-nothing	ABSENT	lisp/nova-work	-	report --act stopped --subject node:D over a leased D prints holder=<name>; the lease stands and reads held-not-worked once its heartbeat leaves --window; no attempt is reconciled; an event --kind cancel on D is admitted or refused exactly as it was before the report
182	a-report-of-a-stop-ends-the-engagement-a-launch-report-began	ABSENT	lisp/nova-work	-	over an unleased :todo D with N open: report --act launched --subject node:D, and D's row prints needs-broken=true; then report --act stopped --subject node:D, and with no other command D's row prints needs-broken=false unmet=1 and check counts one fewer; a second launch report raises it again and a correct --node D lowers it; a stop report on a D that is leased leaves needs-broken=true, because the lease engages it by itself
183	reports-are-read-in-one-ask	ABSENT	lisp/nova-work	-	three reports, one with --instead-of - and one the launch of the replay above: query --ask reports --branch open --since <rev> prints reports=3 no-verb=1 launched-unmet=1 and three kind=report rows, newest last; --max 1 prints one row and a MORE line; --node D prints D's alone; --since before the retention boundary refused as handoffs --since is; --branch closed is exit 2; a report whose --acted-at is a year old is in the answer for the revision it was written at and no other
TABLE
<<<ISSUES
ISSUE 1
TITLE: Wire the #785 needs gate into every admission verb and add the ready invariant tests
RUNG: pro
PACKAGE: lisp/nova-work (src/needs.lisp, src/dependencies.lisp; admission verbs in src/kernel.lisp and src/node-verbs.lisp)
COVERS: work-g-169, work-g-171, work-g-174, work-g-175
BODY:
The replays of the dependency gate (docs/SPEC-WORK.md:6948, 6967, 6983, 6987) demand the
admission-verb gate itself, which the predicate file's own header defers: "rule 3's gate at
the admission verbs is the next slice" (lisp/nova-work/tests/replays-785-gate.lisp:1-8). The
needs-met predicate (src/needs.lisp) and its five reason tokens are built and tested, but no
admission verb enforces it yet, and the ready row invariants are untested.
- `every-admission-verb-refuses-an-unmet-need` — docs/SPEC-WORK.md:6948 "over D with N open and
  no lease, offer or allocation on D: `take --node`, `take --node --for <name>`, `offer`, `state
  --to doing`, `goal update --progress` with D the scope's goal, `take --machine` naming an offer
  that does not exist, and `task packet`; and over a D that was leased, offered and received
  while N was met and whose N was then reopened: `release --handed`, `reassign`, `acknowledge
  --stage accepted`, `take --machine` on the real offer, `task packet`, and `undo` of D's
  earlier `release`. Each exits 1 with its own `FAIL` line ending `unmet need N <reason>`".
  Each admission verb must be tested refusing with `unmet need N <reason>` at exit 1, the
  no-such-node and unknown-offer refusals kept ahead of the gate by rule 3's order, and nothing
  written (no lease, offer, reservation, allocation, W entry, transition, compensating envelope
  or packet file). The `--dry-run` calls must print the same refusals; with N met each must
  print its `OK`; with a scheduling hold over D, `offer` prints `held by control <id>` and the
  acceptance is retained `:accepted-held`, left unconverted by `execution reconcile` while N is
  unmet.
- `a-verb-that-can-admit-declares-its-needs-gate` — docs/SPEC-WORK.md:6967 "the schema file's
  coverage test, over a fixture schema: a verb entry whose event kinds include `:lease` and which
  carries no `needs-gate` fails the test naming the verb; the same entry with `needs-gate:
  refuses` passes; `needs-gate: exempt` with no reason fails; an `acknowledge` entry marked
  `refuses` whose `needs-gate-forms` omits `--stage accepted` fails; and the shipped file passes
  with every verb of rule 3's list marked `refuses` and `execution reconcile` alone marked
  `withholds`." A coverage test must be added that reads the schema/verb table and refuses any
  admitting verb without a `needs-gate` mark, and passes on the shipped file.
- `ready-true-implies-needs-met-and-not-the-reverse` — docs/SPEC-WORK.md:6983 "D needs-met and
  leased by another name: its `ready` row prints `ready=false unmet=0` with an ownership reason
  and `take --node D` prints `LEASE FAIL … : held`; across the whole fixture no row prints
  `ready=true` beside an `unmet=` above zero." The ready row must be tested over a leased
  needs-met node, and the no-ready-while-unmet invariant asserted across a whole fixture.
- `needs-met-is-read-not-released` — docs/SPEC-WORK.md:6987 "N's evidence verified in the cache,
  then `state --to done` on N: after that `OK` line and with no further command the row of D,
  unowned, prints `unmet=0 ready=true`, and the journal holds no event whose `:node` is D; the
  same for a `dep --remove` of D's last unmet edge." A test must assert readiness is derived —
  the journal gains no event on D when N settles or the last edge is removed.
In build order: (1) mark the verb table with `needs-gate` and add the schema coverage test
(work-g-171); (2) call the needs predicate at the candidate gate of every admission verb and
its forms (work-g-169); (3) add the ready-row ownership and no-ready-with-unmet assertions
(work-g-174); (4) assert readiness is read, never an event (work-g-175).
ENDISSUE
ISSUE 2
TITLE: Add the engaged-dependent needs-broken replays of the #785 gate
RUNG: pro
PACKAGE: lisp/nova-work (src/needs.lisp, src/dependencies.lisp; the needs-broken propagation)
COVERS: work-g-170, work-g-176, work-g-177
BODY:
The gate's engaged-dependent replays are not covered: the predicate file tests only rules 1-2
(needs-met and its reason tokens), and lisp/nova-work/tests/replays-8681.lisp
(`reverting-a-need-marks-dependents-needs-broken`) asserts only a weak subset — a dependent is
flagged and unready — never the full engagement story the spec names.
- `a-reopened-need-under-a-doing-dependent-leaves-the-set-green` — docs/SPEC-WORK.md:6963 "D
  `:doing` on N met; `event --kind reopen` on N; then `check`, a `clip` and a fresh `session
  start` on that clip: each prints `findings=0` and no `WORK FAIL` line names D or rule 10,
  `WORK OK` prints `needs-broken=1`, and an unrelated `evidence` on another node is admitted
  after each."
- `reverting-a-need-flags-an-engaged-dependent-and-kills-nothing` — docs/SPEC-WORK.md:6991 "D
  leased, `:doing` and heartbeating on N met; E, a second dependent of N, settled in C; F a
  dependent of E. `event --kind reopen` on N: D's row prints `reason=need-reverted
  needs-broken=true`, `check` prints `WORK OK … needs-broken=2` at exit 0 with no `WORK FAIL
  <id>` line, D's lease, attempt and next `heartbeat` stand, `task packet` for D is refused by
  rule 3, E stays in C with every count of its scope unchanged, and F's row prints
  `needs-broken=false`; N settled and verified again, D and E read `needs-broken=false` with no
  command naming either."
- `an-edge-added-under-work-flags-and-every-way-past-a-need-is-recorded` — docs/SPEC-WORK.md:6998
  "`dep --add M` of an open M onto leased D prints `DEP OK … change=add need=M met=false unmet=1
  needs-broken=true` and stops nothing; onto an unengaged `:todo` node it prints
  `needs-broken=false`; `dep --remove N`, `take --node D`, `dep --add N` with N still open
  leaves three events with their authors, the lease standing and `needs-broken=true`; `dep --add
  D` on D and an edge closing a two-node cycle are each refused `rule 3`, an id naming nothing
  `rule 2: dangling`, each exit 1 with nothing written; a need under another repository work set
  of the same O is admitted; and over a container need with one unverified member, `node require
  --to false` on that member prints its `:require` scope event, moves the container's `required=`
  by one and leaves the dependent `unmet=0`."
A card must: (1) propagate needs-broken to engaged dependents (leased/doing/heartbeating) on a
reopen, keeping lease, attempt and heartbeat standing and refusing `task packet` by rule 3;
(2) record every way past a need as events and assert `DEP OK`/`REPORT` lines; (3) assert the
validation stays green (findings=0, no WORK FAIL naming D or rule 10) through check, clip and a
fresh session on the clip, with needs-broken counted in `WORK OK`.
ENDISSUE
ISSUE 3
TITLE: Build the #854 hand-report verb and its asks
RUNG: pro
PACKAGE: cmd/nova-work (CLI verb) with a kernel symbol in lisp/nova-work (the :report event)
COVERS: work-g-178, work-g-179, work-g-183
BODY:
The hand report is SPEC-AHEAD: cmd/nova-work/socketverbs.go:1003 marks `report` as "SPEC-AHEAD
(nova-tools#854) ... no session answers it yet", and cmd/nova-work/specverbs_test.go:56 pins
the same. The replays demand the verb, its event, its refusals and its ask, all untested.
- `report-records-and-changes-nothing` — docs/SPEC-WORK.md:7007 "`report --act other --subject
  machine:<id> --what … --acted-at <t> --instead-of - --reason …` under `--now <t + 90s>` prints
  `REPORT OK … subject=machine:<id> act=other instead-of=- acted-at=<t> lag=90s unmet=- need=-
  holder=- …`; the journal gains one `:report` event with `:node (:absent)` and its six fields
  in order; `|O|`, W, every lease, every count, every roadmap and the machine's member are
  unchanged; a retry of the request id prints the original line, a changed payload under it is
  refused `reused with a different payload`, an `undo` of it is refused `not reversible here`,
  and two serializers digest it to one value with `:unmet` and `:need` outside the digest."
- `report-refuses-by-name` — docs/SPEC-WORK.md:7015 "each of the six flags missing, an `--act`
  outside the three, a `--subject` with no `:` or of an unknown kind, and a whitespace-only
  `--what`, `--reason`, `--instead-of` and `external:` text: each exit 2 on one line ending
  `run: nova-work help`; a subject naming no node, machine, friend, route or offer, an `--as`
  that is no friend, an `--acted-at` ahead of the clock past `--skew`, and a stale `--expect`:
  each `REPORT FAIL` at exit 1; and in every case no event, no journal record and no dedup
  entry."
- `reports-are-read-in-one-ask` — docs/SPEC-WORK.md:7038 "three reports, one with `--instead-of
  -` and one the launch of the replay above: `query --ask reports --branch open --since <rev>`
  prints `reports=3 no-verb=1 launched-unmet=1` and three `kind=report` rows, newest last;
  `--max 1` prints one row and a `MORE` line; `--node D` prints D's alone; `--since` before the
  retention boundary is refused as `handoffs --since` is; `--branch closed` is exit 2; and a
  report whose `--acted-at` is a year old is in the answer for the revision it was written at
  and no other."
A card must: (1) add the `:report` event with its six ordered fields and `:node (:absent)` to
the lisp kernel, kept out of the digest; (2) wire `nova-work report` in cmd/nova-work with the
write flags and the exit-2 `run: nova-work help` refusals; (3) add `query --ask reports` with
the `reports=`/`no-verb=`/`launched-unmet=` fields, `--max`, `--node`, `--since` and `--branch
closed` behaviours; (4) assert REPORT OK records nothing and dedup/retry/undo behave as the
replay says.
ENDISSUE
ISSUE 4
TITLE: Build the #854 report's launch and stop interplay with the needs gate
RUNG: pro
PACKAGE: cmd/nova-work (report verb) with the needs gate in lisp/nova-work
COVERS: work-g-180, work-g-181, work-g-182
BODY:
The report's hand-launch and stop replays (SPEC-AHEAD #854, no kernel symbol per
cmd/nova-work/socketverbs.go:1003) tie the report verb to the needs gate, and are untested.
- `a-hand-launch-is-refused-when-asked-and-recorded-when-told` — docs/SPEC-WORK.md:7021 "over D
  with N open: `take --node D --dry-run` and `take --node D` both refuse at exit 1 and no flag
  changes either; `report --act launched --subject node:D …` prints `REPORT OK … unmet=1 need=N
  holder=unowned`, writes no lease and no transition, leaves `who` unchanged, and D's row then
  prints `needs-broken=true`; the next `take --node D` still refuses; N met, D's row prints
  `needs-broken=false` and `take` is admitted. The same report over a needs-met node held by
  another name prints `unmet=0 need=- holder=<name>`."
- `a-report-of-a-stop-releases-nothing` — docs/SPEC-WORK.md:7028 "`report --act stopped --subject
  node:D` over a leased D prints `holder=<name>`; the lease stands and reads held-not-worked
  once its heartbeat leaves `--window`; no attempt is reconciled; and an `event --kind cancel`
  on D is admitted or refused exactly as it was before the report."
- `a-report-of-a-stop-ends-the-engagement-a-launch-report-began` — docs/SPEC-WORK.md:7032 "over
  an unleased `:todo` D with N open: `report --act launched --subject node:D`, and D's row
  prints `needs-broken=true`; then `report --act stopped --subject node:D`, and with no other
  command D's row prints `needs-broken=false unmet=1` and `check` counts one fewer; a second
  launch report raises it again and a `correct --node D` lowers it; a stop report on a D that
  is leased leaves `needs-broken=true`, because the lease engages it by itself."
A card must, on top of issue 3's verb: (1) make the launched/stopped reports move a node's
needs-broken flag while writing no lease, no transition and no `who` change; (2) assert a stop
report releases nothing (lease stands, no attempt reconciled, cancel unaffected); (3) assert the
launch-then-stop engagement ends and the counts follow, with a leased D staying needs-broken.
ENDISSUE
ISSUES
<<<READINESS
VERDICT: READY
The first card to cut is ISSUE 1 — wire the #785 needs gate into every admission verb
(work-g-169, 171, 174, 175); its predicate and reason tokens are already built and tested in
lisp/nova-work/tests/replays-785-gate.lisp, so the work is the gate and its tests, and it
unblocks ISSUE 2's engaged-dependent replays and ISSUE 4's report-launch interplay.
READINESS

git status --short
files changed: none
head 5298f6be12eaa0f7e6622334d2b6a1eb427649e3
Left owed: the `## Output grammar` section (lines 5919-6110) is read but not enumerated as behaviours — its demanded shapes are exercised through the named replays listed here; the grammar paragraphs were checked for coverage only where a named replay tests them.
