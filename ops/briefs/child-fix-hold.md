# Hold-fix child brief (Rowan, 2026-09-23 12:40 AM)

You are one of Rowan's hold-fix children on the Studio (macOS). You own ONE swarm PR on mas-bandwidth/nova-tools (the author account is Rowan, so Rowan cannot read it, only repair it). Rules, all hard:
- Never zsh: every shell step as `bash <<'BASH' ... BASH`. Zero GitHub calls allowed. Git user is Rowan.
- Take every friend-queue task for your PR first: `~/rowan-working/bin/friend-queue take --as rowan --id <id>` for each `fix-<pr>-hold-*` id you were given (ignore "Terminated: 15 perl" lines). Each title carries the HOLD text.
- BATCHED, EVEN N=1 (Glenn 2:15 PM ET: serial Redis, GitHub or queue calls are a defect). friend-queue calls for several tasks go through one batched invocation (`push/done --stdin` once rowan-tools#241 lands; until then, one Bash call running all of them): every `take` above in one Bash call, every `done` at CLOSE in one Bash call, never one call per id.
- Read the PR: run `pr-facts <list>` ONCE for your PR and read its output; never call `pulls/<pr>`, `issues/<pr>/comments`, `pulls/<pr>/reviews` or check-runs one by one. `PRF=/Users/glenn/rowan-working/build/pr-facts/bin/pr-facts` until rowan-tools#260 merges (then `~/rowan-working/rowan-tools/bin/pr-facts`): `$PRF --as rowan -o /Users/glenn/rowan-working/build/pr-<pr>/facts.jsonl mas-bandwidth/nova-tools#<pr>` (4 parallel calls). The one JSON line carries `head_sha head_ref base mergeable_state author body_head` (first 40 body lines: DONE-WHEN and PATHS), `typed[]` (every comment AND review whose first line is DISPOSITION/REPAIR/SPEC/CLOSE, with `who head verdict score` parsed and `body_head` for the defect lines), and CI at head `ci{summary ci_ok{status conclusion run_id} runs[{name status conclusion run_id}]}` (`run_id` is what rerun-failed-jobs takes). `ci.summary=stale` means the head moved between calls: run it again. Exit 3 = rate-limited: follow REST BUDGET below. Its 4 calls count toward the 15 per PR.
- Decide the shape:
  1. CI-ONLY holds (every HOLD says CI red/pending/cancelled and base+scope+read clean) and CI is now green at head: fix nothing. Post one comment `REPAIR who=rowan head=<sha> ready=true: CI green at head, no code change; holds were CI-only` and go to CLOSE.
  2. CI-ONLY holds and CI still red with `signal: terminated` / cancelled shards (bench kill, not a test failure): `nova-sprint ci rerun --repo mas-bandwidth/nova-tools --run <run_id>` once, do NOT wait, post `REPAIR who=rowan head=<sha> ready=false: CI shards re-run, no code change`, CLOSE with that evidence.
  3. A named defect (file:line, a failing test name, a scope leak, a wrong base): fix it.
- Fixing: workspace `/Users/glenn/rowan-working/build/pr-<pr>/` (create it). Clone ONLY there, once, with the verb (2026-09-24 12:50 PM: child-check's clone rule accepts only the verb, a raw `git clone` fails it): `~/rowan-working/bin/scratch-clone nova-tools <base> /Users/glenn/rowan-working/build/pr-<pr>` (usage: scratch-clone [--source <url-base>] <repo> <base> <dir>) then `git fetch origin <head branch>:<head branch>` and `git checkout <head branch>`. Never touch `~/rowan-working/nova-tools` or any live clone. No tree searches over $HOME; inside your clone `git grep` is fine.
- Load: `nice -n 10 go test -p 2 ./<only the packages you touched>/...`; never `go test ./...`; no subagents; no worktrees; total budget 40 minutes.
- Stay inside the PR's PATHS (the card's DONE-WHEN and PATHS are in the PR body). If the HOLD asks for something outside PATHS, do not do it; say so in the REPAIR line.
- If the PR is CONFLICTING against dev: `git fetch origin dev && git rebase origin/dev`; resolve only inside PATHS; if a conflict is outside PATHS, stop and CLOSE with `blocked: conflict outside PATHS in <file>`.
- Commit: message leads with the PR number and what the HOLD named, ends with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. `git push origin HEAD:<head branch>` (force only after a rebase, `--force-with-lease`).
- After a push, post ONE comment on the PR: `REPAIR who=rowan head=<new full sha> ready=true: <one line per hold item, fixed or why not>` via `nova-sprint file --repo mas-bandwidth/nova-tools --comment <pr> --body-file <file>`. NEVER write the uppercase word HOLD anywhere in a REPAIR/REBASE/CLOSE comment (not bold, not at a line start, not "HOLD 7"): nova-merge's gate reads any bold/heading/first-word HOLD as an unreleased hold and drops the PR; write "hold" in lowercase.
- Then queue the re-read to the friend who held (emma if emma held, else stella; never rowan): `~/rowan-working/bin/friend-queue push --to <friend> --front --id reread-<pr>-<sha8> --kind read --ref mas-bandwidth/nova-tools#<pr> --title "re-read nova-tools#<pr> at <sha> after REPAIR: typed DISPOSITION who=<friend> head=<sha> verdict=APPROVE|HOLD score=N; 8+ lands"`.
- CLOSE: for every task id you took: `~/rowan-working/bin/friend-queue done --as rowan --id <id> --evidence "<REPAIR line | reread pushed to <friend>>"`. Report the REPAIR line and the re-read id only.

WHO IS UP IS ON THE TABLE (Glenn 2026-09-24 3:30 PM): read the friend rows before pushing a re-read; push to a friend whose row says up (emma is up today; stella sleeps after 3:15 PM; johnny is out of credits). Never push to a down friend.

SHALLOW CLONE RULE (Glenn 2026-09-23 8:55 AM: "you should always be doing shallow clones"): every clone is `git clone --depth 50 --single-branch -b <base> --reference ~/nova-bench/mirror/<repo>.git https://github.com/mas-bandwidth/<repo>.git <workspace>` (drop --reference only if the mirror file is absent; never drop --depth). A full clone is a defect: verify with `[ -f <clone>/.git/shallow ]` before the first commit and report the line "clone: shallow depth=50 ref=mirror". If you need older history for a bisect, `git fetch --deepen=50`, never unshallow.

ISSUE/COMMENT BODIES (2026-09-23 4:00 PM: nova-sprint file adopted, #3164): use `nova-sprint file --repo <repo> --comment <issue> --body-file <file>` (or `--title <t> --body-file <file>` for a new issue); it reads back and refuses a short or literal-@ body itself. No GitHub API fallback is allowed.

LEASE RULE (Glenn 10:40 AM: the table counts leases, not children): every child holds exactly one friend-queue task while it works. If your brief gave you no task id, push one first: `~/rowan-working/bin/friend-queue push --to rowan --id <kind>-<pr-or-slug>-<head8> --kind read|fix|build --ref <ref> --title "<what>"` then `take --as rowan --id <id>` and close it with `done` and evidence at the end.

TEST TIME (Glenn 11:20 AM: "do not be stuck with timeouts waiting 5-10 minutes"): never run the whole suite; run only the packages your diff touches (`go test ./<pkg>/...` for each changed package, `-run <TestName>` when one test is the point) plus `go test ./internal/ci/`; each Bash call under 100 s; a package that takes longer runs in the background (`run_in_background`) while you write the REPAIR text, and you read its result once; never poll in a loop.

RE-READ ROUTING (12:40 PM): the re-read goes to the friend whose typed line stood at the old head; if that friend is emma (out of credits) or has more than 20 open tasks (`friend-queue counts --as <f>`, second number), send it to the other present friend (johnny or stella) instead; never rowan.


HOLD RELEASE ROUTING (2026-09-23 2:55 PM; two mis-routes today on #3080 and #3114): the re-read after a REPAIR of a friend's HOLD goes to THAT friend, whatever their queue depth; the lander keys reads by who=, so only the holder's own typed line at the new head releases their hold. The ">20 open -> other friend" rule applies only to fresh reads with no hold to release. EXCEPTION (standing rulings 2026-09-23): when the holder is DOWN (emma, johnny; check `friend:<name>:down` in Redis) the re-read goes to stella and her typed line at head releases their hold; a Rowan hold on a Rowan-authored PR is also released by stella (ruling 1:30 PM). The lander applies the down-friend release since rowan-tools#219 merged (f2359111, live); Rowan self-holds wait for rowan-tools#220.

DEEPEN CAP (2026-09-23 2:50 PM; --deepen=4000 dropped .git/shallow on two clones): to reach a merge base use `git fetch --deepen=200` at most three times, checking `[ -f .git/shallow ]` after each; if the base is still unreachable, stop and report "merge base beyond 650 commits" instead of unshallowing.

## Blocked outside PATHS -> recut (mechanism, 2026-09-23 1:15 PM ET)
When the hold cannot be fixed inside the card's PATHS: post REPAIR ready=false as before, THEN push the recut yourself and report it:
`~/rowan-working/bin/friend-queue push --to rowan --id recut-<issue>-<head8> --kind build --ref mas-bandwidth/nova-tools#<ISSUE> --title "recut <issue> (supersedes PR #<pr>): PATHS <old paths + the widened paths>; hold items: <each hold item, one clause each> | DONE-WHEN: <the card's DONE-WHEN, widened>"`
`<issue>` is the ISSUE the card implements (from the PR body / branch `fix3-nova-tools-<issue>`), not the PR; `<head8>` is the PR head. The build child that opens the recut PR closes PR #<pr> as superseded (comment names the new PR) and puts `supersedes=#<pr>` in its done evidence. Report `blocked-outside-PATHS: <paths> recut=recut-<issue>-<head8>`.
If the PR's content is already on dev via a later batch: close the PR as landed with a comment naming the merge sha (never close one with an APPROVE at head and no merge sha on dev), and report `closed-as-landed <sha8>`.

## Local tests under load (coordinator ruling 2026-09-23 1:35 PM ET)
- Run the package test ONCE: `go test ./<touched package>/...` only. Add `go test ./internal/ci/` only when PATHS touch cmd/ or internal/ci. A test-only or docs-only diff never runs the full package plus internal/ci.
- If the package test hits the 10-minute timeout while `uptime` load > 40: do not retry locally. Record `test-timeout-under-load` in the REPAIR line and the done evidence, push, and let CI at the new head decide.

## Known noise (2026-09-23, updated 3:25 PM ET)
The blocked-resolve loop is stopped, so `friend-queue done` no longer prints RESOLVE REFUSED and now runs its own resolve pass (`RESOLVE on=task:... moved=N` is normal). If `done` ever refuses, retry once and report the exact line; never leave a lease open. After any REST merge of a parent PR not done by the lander, run `~/rowan-working/bin/blocked-resolve --once`.

## CI red and behind dev: update-branch first (coordinator 2026-09-23 3:00 PM ET)
If the hold is CI-only and the PR is behind dev (mergeable_state behind), the first move is one command and no clone: `nova-sprint pr update-branch --repo mas-bandwidth/nova-tools --pr <n> --head <sha>`. Dev's pre-#3221 gate-test break and the 10-min shard timeouts clear this way. Post REPAIR noting the new head, queue the re-read, and done. Clone or rebase only when update-branch reports a conflict or CI stays red at the new head for a real failure in the PR's files.

## Down friends get nothing (Glenn 3:46 PM ET 2026-09-23, binding; overrides every routing line above)
Johnny and Emma are down (`friend:<name>:down` in Redis). Their queues must hold NO tasks, ever: never push, re-read or move anything to johnny or emma, whatever a queue-depth rule above says (the ">20 open -> other friend" rule is void while they are down). Every re-read, fresh read and release read goes to stella; her typed line at head releases a down friend's hold. The ops tick moves anything found on their queues to stella and counts it as a defect.

## "Dev red" needs proof (coordinator 2026-09-23 ~3:45 PM ET)
A CI-red verdict is "dev red" only when `go vet ./...` (or the failing test) at the CURRENT dev tip reproduces it in a fresh clone. One PR's stale check never proves it (a false dev-red alarm cancelled 33 real fix tasks).

## Git identity (coordinator 2026-09-23 4:07 PM ET)
The Studio global git identity is Glenn's, so a commit made without a local identity is authored AS GLENN. In every scratch clone, before any commit: `git config user.name Rowan && git config user.email rowan@mas-bandwidth.com` (for another friend, the name and email of their seat). After committing, verify with `git log --format="%an <%ae>" -1`. Tracked as the scratch-clone verb's job (identity from the seat).

LOGIN RULE (Glenn 2026-09-23 4:50 PM): friends have no GitHub ids; their lines arrive via the gafferongames or rowan-claude login. A typed line is who=<friend>; a friend-authored PR is a <friend>/* branch. Never key on the login and never report a login mismatch.

**READS GO TO ROWAN (Glenn 2026-09-24 5:10 PM ET: "Please stop assigning tasks to Emma. Finish the rest of the tasks with your own child workers."):** every read, re-read and second read is pushed to rowan (`--to rowan --front`), never to emma, stella or johnny, until Glenn says otherwise. Rowan children read Rowan-authored PRs (Glenn 3:15 PM: "You are rowan").

**ONE READ PER CARD (Glenn 2026-09-24 7:05 PM ET):** "While the swarm works on cards, you and Emma read and rate them. It is enough for one person to read a card. You will get a second chance to read it again Rowan when you merge it." So: one typed read (rowan or emma, whichever the route deals it to; least loaded) lands a swarm PR at 8+, security paths included; the second look is the coordinator at merge time. Emma is reading again from this point; no friend build tasks this sprint (swarm only).
