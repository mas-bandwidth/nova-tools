REPAIR who=rowan head=27bb138d129ddf683993644c3d9c6b100c06362b ready=true: stella's hold at 8a099710 (hands rows lack the exact command or decision) fixed; rebased on dev

- stella hold (score 7, comment 5799659915). The Glenn's-hands `Wanted` values were labels, not #2065's exact command-or-decision field. Fix: the hands table gains an "Exact command or decision" column. #2065's own text and the repo hold only the labels for the 2026-09-20 set; the sshd commands, the bench-user decision, the Go version, the hetzner key scopes and caps, the darwin-x64 download and the spend limits were chat messages. So each of H-01..H-06 now says `unavailable:` with where the words were, and its State is marked **incomplete**. The section defines the distinction: an incomplete row is an index entry a handover can neither act on nor verify; the only act on it is to re-ask Glenn for the exact wording and record it; it cannot move to done or verified until the field is filled.
- pinned in the focused test (red at 8a099710, green at head): TestIssue2065 now parses every `| H-` row. It requires six cells, `unavailable:` exactly when State carries **incomplete**, a filled field quoted from its source, never done or verified while incomplete, and all six rows present.
- scope: ROADMAP.md and internal/docs/issue2065_test.go only (PATHS). The primary RULINGS form/verb and `nova-pulse hands` remain the separately identified follow-up, as stella notes.
- tests: `go test ./internal/docs/ -count=1` ok (1.7 s); `go test ./internal/ci/` ok (60.2 s, ci-rules: OK).
- rebase: onto dev 34160ca7d (the branch was 204 commits behind); no conflicts.

