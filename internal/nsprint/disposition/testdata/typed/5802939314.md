REPAIR who=rowan head=93b903042a200394a9a13596e7aaa47e79df5534 ready=false: stella's source finding fixed at this head; CI red only from the dev break tracked in #3342 (outside PATHS)
- stella's hold (comment 5800295378, extra-column receipt row widens a batch): fixed in 93b90304, internal/pulse/kinds.go acceptedFirstPairs now requires len(f) == 3; regression subtest "an extra-column ACCEPT OK row leaves the pair unproven" in internal/pulse/issue2215_test.go. It fails on the old len(f) < 3 check (PULSE OK n=2, exit 0) and passes on the fix (go test -run TestIssue2215 ./internal/pulse/ ok 2.4s).
- CI at this head: lint (vet) and test (1/4 space) (vet) are red on internal/pulse/fill_dependency_test.go:171, where recordLauncherWithSeq implements the 2-arg Launch but CardLauncher wants 3. That file is not in this PR, and go vet ./internal/pulse/ reproduces the same failure at dev tip 24f0e1d7e in a fresh clone, so dev is red (issue #3342). The local test run above applied that one-line test-double fix without committing it.
- scope: the PR diff against its base is only internal/pulse/issue2215_test.go, kinds.go and launch.go (PATHS). The branch is behind dev; once #3342 lands, update-branch and CI at the new head decide.

