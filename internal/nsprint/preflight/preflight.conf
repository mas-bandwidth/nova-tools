# The preflight windows (#2947 rev 3). sprint open (#2939) copies them into
# s:<S>:policy; preflight reads them from there and never defaults a missing
# one (7.9 is RED instead).
start_window_s=60
beat_stale_s=120
reconciler_pass_s=20
interim_window_s=600
