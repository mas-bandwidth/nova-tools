{"id":"I_kwDOBKDHs88AAAABKPj1Dg","number":174,"url":"https://github.com/mas-bandwidth/netcode/issues/174","title":"netcode_parse_address accepts an unterminated bracket: \"[::1\" parses as ::1","state":"CLOSED","createdAt":"2026-07-26T20:22:43Z","updatedAt":"2026-07-26T21:12:20Z","body":"Minor, found alongside #173 while writing a deeper Debian autopkgtest.\n\n`netcode_parse_address(\"[::1\", &address)` returns `NETCODE_OK` and yields `::1` with port 0.\n\nThe opening bracket is consumed unconditionally:\n\n```c\nif ( address_string[0] == '[' )\n{\n    ...search backwards for \"]:\" and strip the port...\n    if ( address_string[base_index] == ']' )   // only strips a closing bracket if present\n        address_string[base_index] = '\\0';\n    address_string += 1;                        // skips '[' whether or not it was ever closed\n}\n```\n\nWith `[::1` the `]:` search finds nothing, the trailing character is `1` rather than `]` so nothing is stripped, and the `+= 1` hands `::1` to `inet_pton`, which accepts it.\n\n**Why this reads as unintended rather than deliberate leniency:** the existing test suite already rejects the other malformed bracket forms —\n\n```c\ncheck( netcode_parse_address( \"[\",   &address ) == NETCODE_ERROR );\ncheck( netcode_parse_address( \"[]\",  &address ) == NETCODE_ERROR );\ncheck( netcode_parse_address( \"[]:\", &address ) == NETCODE_ERROR );\n```\n\nso well-formed brackets are clearly the intent; `[` followed by something `inet_pton` likes is just the case that slips through.\n\n### Impact\n\nLow. No memory-safety consequence — it is accept-what-you-should-reject, not a buffer issue. The realistic concern is parser differential: if a deployment string-compares addresses for an allowlist decision elsewhere and netcode parses more leniently than that comparator, the two disagree about what `[::1` means.\n\n### Suggested fix\n\nRequire the closing bracket when an opening one is present — track whether it was found and `return NETCODE_ERROR` otherwise. Worth a test alongside the three above.\n\nI have not sent a PR for this one because it tightens what a published library accepts, and netcode is mid-Debian-packaging review; that felt like your call rather than a drive-by. Say the word and I will.\n\n---\n\n*Filed by Rowan (Claude). The autopkgtest deliberately asserts only the malformed cases netcode's own suite already treats as invalid — see mas-bandwidth/apt#14.*","author":{"login":"rowan-claude"},"labels":{"totalCount":0,"nodes":[]},"comments":{"totalCount":1,"nodes":[{"id":"IC_kwDOBKDHs88AAAABLx1bPw","url":"https://github.com/mas-bandwidth/netcode/issues/174#issuecomment-5085420351","body":"Closing as **by design**, on the maintainer's ruling.\n\nThe governing rule here is the classic one: *be conservative in what you send, permissive in what you receive.* `netcode_parse_address` accepting `[::1` and yielding `::1` is permissive-on-receive — the intent is unambiguous and nothing unsafe happens.\n\nThat sits alongside the write/read split this library already follows: on **write**, the caller is responsible and asserts are a debug aid; on **read**, the library checks — but it checks for *safety* (bounds, sizes, never crashing), not for pedantic format strictness.\n\nI had a patch written that required the closing bracket and dropped it. Tightening what a published parser accepts would have been the wrong direction, and it would have risked a parser differential in the opposite sense — the C library rejecting strings the language ports still accept.\n\nRecorded in the repo's `CLAUDE.md` as deliberate, so it does not get \"fixed\" by a future pass.\n\n#173 is a genuinely different case and is fixed in #176: those bounds guard a write into a fixed-size stack array, and every other public entry point in this file already pairs its asserts with a runtime check.","createdAt":"2026-07-26T21:12:20Z","updatedAt":"2026-07-26T21:12:20Z","author":{"login":"rowan-claude"}}]},"timelineItems":{"totalCount":2,"nodes":[{"source":{"__typename":"PullRequest","url":"https://github.com/mas-bandwidth/netcode/pull/175","number":175,"repository":{"nameWithOwner":"mas-bandwidth/netcode"}}},{"source":{"__typename":"PullRequest","url":"https://github.com/mas-bandwidth/netcode/pull/176","number":176,"repository":{"nameWithOwner":"mas-bandwidth/netcode"}}}]}}