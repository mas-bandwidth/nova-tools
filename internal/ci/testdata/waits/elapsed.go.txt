package fixture

import (
	"testing"
	"time"
)

func TestFixtureElapsedAssertion(t *testing.T) {
	start := time.Now()
	if time.Since(start) < 5*time.Second {
		t.Fatal("the clock, not the event")
	}
}
