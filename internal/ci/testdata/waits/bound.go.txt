package fixture

import (
	"context"
	"testing"
	"time"
)

func TestFixtureShortBound(t *testing.T) {
	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()
	<-ctx.Done()
}
