Score this pull request from 1 to 10 as a reviewer would, where 1 is the worst and 10 the best. The change is one cell of a conformance matrix: a single added test that must exercise the generated code for one law, in the paths its card declares, with a negative control that really goes red. A test that builds the bytes it then asserts, re-declares the type it claims to test, compares against a reference pasted into the file, or asserts a literal true scores 3 or below however well it is written.
