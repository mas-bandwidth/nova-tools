# Command reference

The fixture command reference: the size of a first run, and the one the tests
run the banner's dogfood example against.

## nova-example

```
nova-example quickstart --dir <dir>
nova-example links --dir <dir>
nova-example nocode --dir <dir>
```
