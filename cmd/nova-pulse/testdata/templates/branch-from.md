RESULT <label> sha=<sha12>
You are a worker. The deadline is the machinery's.
Do not run go build, go test or any toolchain; read and write only.
STEP 1. mkdir -p scratch && git clone -q https://github.com/mas-bandwidth/nova-tools.git . && git checkout <branch>
   check: git rev-parse HEAD prints a head.
STEP 2. The pull request is <issue> and <branch> is its exact head, onto <base>.
STEP 3. Read the diff against <base> and write notes.txt: what it changes, and what it misses.
STEP last. Write RESULT.md with line 1 equal to this card's line 1.
