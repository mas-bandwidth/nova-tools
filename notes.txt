CARD-8052 nova-tools #592: nova-version help omits the callable snapshot verb

Red test: TestHelpNamesTheSnapshotVerb (internal/update/rules_test.go)
Failing output (verbatim):
--- FAIL: TestHelpNamesTheSnapshotVerb (0.00s)
    rules_test.go:404: nova-version help omits the snapshot verb:
        nova-version report --file <manifest: one line per tool, six tab-separated fields name kind installed latest apply owner, written by hand> [--host <label>] [--snapshot <path>] [--draft --as <friend> --to <who,who>] [--max <n>] [--timeout <d>] [--budget <d>] [--kind <k>]
        nova-version send --file <manifest: one line per tool, six tab-separated fields name kind installed latest apply owner, written by hand> --as <friend> --to <who,who> --bus <path> --remote <r> --branch <b> [--snapshot <path>] [--host <label>]
        nova-version help
        nova-version version (or --version)

Fix: add `nova-version snapshot --bin <dir> --out <manifest> [--owner <name>]`
as the first line of update.versionVerbs in internal/update/cli.go; leave the
report/send/help lines and the help() trailing lines untouched. Docs updated:
SPEC-UPDATE.md "The verbs" prose (three lines now) and docs/CLI.md nova-version
section (snapshot verb named). Round-trip TestSnapshotThenReportRoundTrips and
TestFriendSequenceSnapshotReport preserved and still green.
